# SOP — Track builder

## Purpose
Closed Catmull-Rom centerline → arc-length LUT → swept road/kerb/barrier mesh, boost pads, pickups, hazards, decor instancing, minimap outline, Final Lap Shift overrides.

## Inputs
TrackDefinition JSON.

## Outputs
Track object: LUT, meshes, checkpoint list, spawn grid, shortcut graph, applyFinalLapShift().

## Constraints
One material per track chunk. Merge statics. InstancedMesh for decor. Under 100 draw calls with 8 karts on screen.

## Tests (must pass before merge)
LUT round-trip: nearestT(pointAt(t)) == t ± 1e-3 for 1000 samples; checkpoints strictly increasing; draw-call count asserted in a headless render.

## References
turbo-kart-rush Centerline.ts and track/builders; three.js CatmullRomCurve3.

## Approach
_Synthesised 19 Sept 2026 from turbo-kart-rush `src/track/*` (Centerline.ts, Track.ts, builders/road.ts, builders/barriers.ts), Mario-Kart-3.js (GLB track + three-mesh-bvh), Starter-Kit-Racing (grid tiles + editor), research plan §4.2–4.4, §6.3–6.4, appendix A§3, A§6, B§4, C§8, first-principles §2, and the `TrackQuery` contract already fixed in `src/kart-controller/types.ts`._

### What we take from the references
- **turbo-kart-rush** is the model. Closed centripetal Catmull-Rom, a 2048-sample arc-length LUT stored as flat Float64Arrays, allocation-free `sample(t, out)`, hint-based local `closestT` with a two-segment projection refinement, half-widths smoothed across control points so seams do not show, hand-swept road/kerb/shoulder ribbons, instanced barriers, checkpoints at `i/N`, spawn grid sampled backwards from the start line, minimap from 200 edge samples. We copy the *shape*. We do not copy: Three.js `CatmullRomCurve3` in the sim (the sim never imports Three.js), the global-search fallback inside the hot path (kart schema says never), the procedural terrain distance field (1600 m grid, out of scope), `getUtoTmapping` at runtime, and its lack of any hairpin guard.
- **Mario-Kart-3.js** bakes the track as a Blender GLB and finds ground with three-mesh-bvh. Rejected as the source of truth: no centreline means no free positions, checkpoints, AI line or minimap, and every Final Lap Shift would be a second GLB. We keep nothing from it for v1. Wall collision does not need BVH either: the kart already clamps to `halfWidth − kartRadius`.
- **Starter-Kit-Racing** builds from a tile grid with a browser editor. Rejected: the editor is deleted from scope (first-principles §2) and tiles cannot do camber, elevation or smooth hairpins. We keep one habit: every decor species is one `InstancedMesh`.
- **Nobody** has shortcuts as branches, a rebuildable route, or banked road from data. Those we design here.

### The model
A `TrackDefinition` (JSON, validated against `track.schema.json`) becomes a `Track` in two layers:
1. **Sim layer** (pure TypeScript, no Three.js, headless-testable): spline → LUT → `TrackQuery`, checkpoints, spawn grid, shortcut branches, baked features, hazards, minimap outline, `applyFinalLapShift()`.
2. **Scene layer** (Three.js, only under `mesh/`): reads the sim layer and builds meshes. Never the other way round.

**Spline.** Our own closed centripetal Catmull-Rom (about 40 lines; same maths as Three.js `centripetal`, so authored points look identical in a debug overlay). Control points carry `x y z halfWidth bank surface`. Segment *i* runs from point *i* to point *i+1*; its `surface` is point *i*'s, its `halfWidth` blends smoothstep from point *i*'s to point *i+1*'s so there is no visible step and no post-filter.

**LUT.** `LUT_SAMPLES = 2048` samples at equal arc length (built by walking the curve with `ARC_DIVISIONS = 4096` fine steps, then resampling). Per sample, flat typed arrays: position (x,y,z), unit tangent, unit horizontal right `(tz, 0, −tx)` (this is exactly the kart-controller's `right`, so lateral offsets agree), bank in radians, halfWidth, surface id, segment index. `length` in metres. `sample(t, lateral)` linearly blends the two neighbouring samples then applies the lateral: `position = centre + right × lateral + up × (lateral × tan(bank))`, `groundY = position.y`, `normal = normalize(cross(rightBanked, tangent))`. Lateral is horizontal metres, so a kart's XZ lateral stays consistent on a banked corner. `wrap01` is applied to every incoming `t`; `sample(1) === sample(0)`.

**Bank sign.** Positive `bank` lifts the outer edge of a right turn, i.e. the left edge (negative lateral) rises. `MAX_BANK_DEG = 20`; validation rejects more.

**nearestT.** Local only, as the kart schema demands: scan integer samples inside `hintT ± window` (0.02 → ±41 samples) on XZ distance squared, take the best, then project the point onto the two adjacent LUT segments for a sub-sample `t`. Deterministic, allocation-free. A separate `nearestTGlobal(position)` (3D distance, coarse step 8 then refine) exists for spawn, respawn, feature baking and tests only. XZ-only local search is what lets Canyon Rush and Skyline Circuit cross over themselves: the window keeps the search on the right level, and only the global search needs Y.

**Shortcuts are branches.** The track holds branch 0 (main) plus one branch per `shortcuts[]` entry, each with its own small LUT. A branch sample is indexed by *main-equivalent* `t = lerp(entryT, exitT, u)` (wrap-aware), so a kart on a shortcut still has ordinary race progress, checkpoints, minimap dot and AI target. `nearest()` searches the current branch first, then any other *open* branch whose `[entryT, exitT]` overlaps the window, and switches only when the other is closer by more than `BRANCH_HYSTERESIS = 1.0 m` in 3D. `openOnLaps` and Final Lap Shift toggle `open`; a kart already on a branch that closes rides it to `exitT` and is never teleported. See open question 1: this needs `branch` on the kart state and a two-argument return from `nearestT`.

**Race helpers.** Checkpoint *i* at `t = wrap(startGrid.t + i / checkpointCount)`, checkpoint 0 is the start line; each carries position, tangent, halfWidth. Spawn grid: `rows × columns` slots walking backwards from `startGrid.t` by `spacing` metres per row, columns spread across `±0.5 × halfWidth`, alternate rows offset by half a column, heading = tangent. Minimap: `MINIMAP_SAMPLES = 200` left/right edge points per open branch, fitted into `[0,1]²` with `MINIMAP_PADDING = 0.06`, plus `toMinimap(position)`.

**Features bake to world.** Pickups (balloons), coins, boost pads, jumps and hazards are *authored* in `t + lateral` but *stored* as world positions at build time. After any LUT rebuild their `t` is re-derived with `nearestTGlobal`, so a shift that changes the track length cannot slide a boost pad down the road. `TrackQuery.jumps` and `boostPads` are views over these baked lists. Boost pads are `BOOST_PAD_HALF_LENGTH = 1.75 m` long, default `width = 3 m`.

**Hazards.** `hazards.ts` owns the deterministic kinematics for the five kinds, driven by race time only: `rolling` (moves along −tangent from `t` at `speed`, respawns every `period`), `crossing` (oscillates laterally across the road every `period`), `falling` (drops from height every `period`, active for 0.5 s on the ground), `static`, `gust` (a `period`-timed lateral push of `speed` m/s² over a 6 m window). It exposes `activeHazards(time) → { id, position, radius, hit, push? }[]`; race-manager does the kart test and calls `applyHit`. Nothing here touches kart state.

**Final Lap Shift.** `applyFinalLapShift()` is idempotent and runs once, when race-manager says the leader started the last lap. In order: (1) `routeOverrides` splice their control points into the main list over `[fromT, toT]` and the main LUT is rebuilt once; (2) every kart is remapped with `nearestTGlobal` at its world position (race-manager passes the list); (3) features re-derive `t`; (4) checkpoints, spawn grid and minimap are recomputed; (5) `surfaceOverrides` overwrite the baked surface ids; (6) `gripMultiplier` scales every sample's `gripScale`; (7) shortcuts open/close, jumps added, hazards enabled/disabled; (8) a `TrackChanged` event carries `sky, lut, fogDensity, musicVariant, label` for the art, audio and HUD systems, and the scene layer swaps the affected chunk meshes (instant swap in v1; the flash and camera shake belong to vfx-juice). The `reverse` kind is a conveyor flag for the stretch track and does not reverse the spline.

**Validation** (`validate.ts`, runs in tests and at load in dev): closed, ≥ 8 points, no NaN; start-line `halfWidth ≥ 4` (four karts of `2 × kartRadius` plus air); every sample's turn radius `≥ MIN_TURN_RADIUS_FACTOR × halfWidth` with `MIN_TURN_RADIUS_FACTOR = 1.5`, which is the hairpin guard turbo-kart-rush lacks (an inner kerb tighter than this folds the ribbon); `|bank| ≤ 20°`; shortcut `entryT < exitT` (wrap-aware) and its branch ends within 2 m of the main line; `checkpointCount ≥ 4`; estimated lap time `length / (0.8 × topSpeed)` inside 40–65 s, else a warning against the design's 45–60 s target; `voidY` below the lowest sample by ≥ 5 m.

**Scene layer.** Hand-built `BufferGeometry` ribbons (appendix A§3: not `TubeGeometry`). Per LUT sample: road (2 verts), kerb each side (3 verts: `KERB_WIDTH = 1.0`, `KERB_HEIGHT = 0.1`), shoulder each side to `SHOULDER_WIDTH = 6 m` falling `SHOULDER_DROP = 0.4 m`. All strips of one `CHUNK` are merged into **one mesh with one `MeshToonMaterial`** using vertex colours from the biome palette plus a road/dirt/mud/ice/kerb colour table; UV `v = arcLength / ROAD_TILE_LENGTH` (10 m) for the road stripe texture. `CHUNK_COUNT = 8` equal-`t` chunks per branch so frustum culling works and a shift rebuilds only touched chunks. Barriers: one `InstancedMesh` per biome barrier asset at `halfWidth + KERB_WIDTH`, every `BARRIER_SPACING = 2.0 m`, visual only. Decor: one `InstancedMesh` per `environment.decor` asset, seeded by `hash(track.id)`, placed in its band (`roadside` 8–14 m from the edge, `far` 30–120 m, `sky` 25–60 m up) and rejected if inside any branch's road envelope. Balloons, coins and boost pads: one `InstancedMesh` each. Start line: one quad with polygon offset. Ground: a single plane at the biome ground height, or none for Skyline (open question 2). Sky: one dome. Draw-call budget for the whole track scene with no karts: `TRACK_DRAW_CALL_BUDGET = 40` (8 chunks + ≤ 8 decor + ≤ 4 barrier/feature instancers + start line + ground + sky + hazards + landmark), leaving 60 for 8 karts, items and post.

**Authoring.** Six JSON files under `src/track-builder/tracks/`, written by hand in metres, plus a dev-only debug overlay (`?debug=track`) that draws the centreline, edges, checkpoints and features. No editor (first-principles §2). Harbour Loop first; it must pass the validator and the integration test before any second track.

### Module boundaries (`src/track-builder/`)
| File | Owns | Test |
|---|---|---|
| `types.ts` | `TrackDefinition` (mirrors the schema), `Track`, `Branch`, `Checkpoint`, `SpawnSlot`, `BakedFeature`, `TrackChanged` event | — |
| `constants.ts` | The frozen builder constants below, plus `kartRadius` and `tSearchWindow` read from the kart schema | values match the schema |
| `spline.ts` | Closed centripetal Catmull-Rom: `pointAt(u)`, `segmentOf(u)`, arc-length walk | matches three.js `CatmullRomCurve3` centripetal on 3 shapes ± 1e-6; closed at u=0/1 |
| `lut.ts` | Build the LUT; `sample`, `nearestT`, `nearestTGlobal`, `wrap01` | round trip, uniform spacing, seam, lateral, bank, surface segment |
| `branches.ts` | Main + shortcut branches, open flags, `nearest()` with hysteresis | on/off-branch selection, closed branch ignored, monotonic `t` through a shortcut |
| `features.ts` | Bake pickups/coins/pads/jumps/hazards to world, re-derive `t` after rebuild | positions stable through a rebuild ± 0.05 m |
| `hazards.ts` | Deterministic hazard kinematics, `activeHazards(time)` | period, positions at time T, determinism |
| `race.ts` | Checkpoints, spawn grid, `distanceAlong` helpers | strictly increasing, N slots on road facing forward |
| `minimap.ts` | Outline polylines, `toMinimap` | in unit square, centre projects onto outline |
| `shift.ts` | `applyFinalLapShift()` | length changes, karts and features remap, idempotent |
| `validate.ts` | Authoring checks above | each rule rejects its bad fixture |
| `track.ts` | `buildTrack(def) → Track` (implements `TrackQuery`) | determinism: build twice → identical bytes |
| `mesh/road.ts`, `mesh/decor.ts`, `mesh/chunks.ts`, `mesh/scene.ts` | Three.js only. Ribbons, kerbs, shoulders, barriers, instancers, chunk merge, `buildTrackScene(track, assets) → Group`, chunk swap on shift | mesh/instancer count ≤ 40 (proxy for draw calls) |
| `debug/overlay.ts` | Dev overlay lines | visual |
| `tracks/*.json` | The six track definitions | each passes `validate` |
| `index.ts` | Public exports | — |

`race-manager` (next system) consumes `Track.checkpoints`, `spawnGrid`, `activeHazards`, `applyFinalLapShift`, `nearestTGlobal` for respawn, and the `TrackChanged` event. `ai-driver` reads `sample(t + lookahead, bias)`. `ui-hud` reads `minimap`.

### Constants
Builder constants (to be added to `track.schema.json` as `builder` defaults before code, schemas first): `LUT_SAMPLES` 2048, `ARC_DIVISIONS` 4096, `GLOBAL_SEARCH_STEP` 8, `BRANCH_HYSTERESIS` 1.0 m, `MAX_BANK_DEG` 20, `MIN_TURN_RADIUS_FACTOR` 1.5, `MIN_START_HALF_WIDTH` 4, `KERB_WIDTH` 1.0, `KERB_HEIGHT` 0.1, `SHOULDER_WIDTH` 6.0, `SHOULDER_DROP` 0.4, `BARRIER_SPACING` 2.0, `ROAD_TILE_LENGTH` 10, `CHUNK_COUNT` 8, `MINIMAP_SAMPLES` 200, `MINIMAP_PADDING` 0.06, `BOOST_PAD_HALF_LENGTH` 1.75, `BOOST_PAD_WIDTH` 3.0, `BALLOON_HEIGHT` 1.2, `BALLOON_RADIUS` 0.9, `COIN_RADIUS` 0.5, `HAZARD_RADIUS` 1.2, `FALLING_ACTIVE_SECONDS` 0.5, `GUST_WINDOW` 6 m, `DECOR_BANDS` roadside 8–14 / far 30–120 / sky 25–60, `LAP_TIME_WARN` 40–65 s, `TRACK_DRAW_CALL_BUDGET` 40.
Read from elsewhere, never redefined: `kartRadius` 0.85 and `tSearchWindow` 0.02 from `kart.schema.json`; `topSpeed` 25 for the lap-time estimate; lap target 45–60 s, width ≥ 4 karts, shortcut gain ≤ 2 s from design §6.

### Tests (headless, vitest, deterministic)
1. **LUT round trip**: `nearestT(pointAt(t), t + noise(±0.01), 0.02) == t ± 1e-3` for 1000 samples, on Harbour Loop and a synthetic figure-eight with an overpass.
2. **Uniform arc length**: neighbouring LUT samples are `length / 2048` apart ± 2%.
3. **Seam**: `sample(1) === sample(0)`; `nearestT` across the seam in both directions.
4. **Lateral and bank**: `sample(t, 3).position − sample(t, 0).position` is 3 m, perpendicular to the tangent, on the kart-controller's right; a 10° bank raises the left edge by `5 × tan 10°` at lateral −5 and the normal tilts the same way.
5. **Surface**: point *i*'s surface holds up to point *i+1*; `halfWidth` blends with no step larger than `1/2048` of the difference.
6. **Checkpoints**: `checkpointCount` entries, strictly increasing from the start line, wrap-aware.
7. **Spawn grid**: `rows × columns` slots, all inside the road by ≥ `kartRadius`, behind the line, heading·tangent > 0.99.
8. **Branches**: a kart on the shortcut gets the shortcut ground; `t` is monotonic through it; a closed shortcut is never selected; a kart mid-branch when it closes finishes it.
9. **Final Lap Shift**: a route override changes `length`; every baked feature's world position moves < 0.05 m; a remapped kart's position is unchanged; surface and grip overrides show in `sample()`; a second call changes nothing.
10. **Validate**: each rule rejects its fixture (hairpin at `1.2 × halfWidth`, start width 3.5, bank 25°, NaN, open loop, shortcut exit 5 m off the line, `voidY` above the road).
11. **Minimap**: outline inside `[0,1]²`; `toMinimap(sample(t,0).position)` within 1/200 of the outline.
12. **Hazards**: `activeHazards(T)` deterministic; `rolling` respawns every `period`; `crossing` stays inside `halfWidth`.
13. **Determinism**: `buildTrack` twice → byte-identical LUTs and decor transforms.
14. **Draw calls**: `buildTrackScene(harbourLoop)` contains ≤ 40 `Mesh` + `InstancedMesh` objects, each with one material. The true `renderer.info.render.calls` assertion with 8 karts joins the headless perf run when the `performance` system adds it to `npm run verify` (today `verify` is `tsc && vitest run`).
15. **Integration**: the kart-controller integration test runs on Harbour Loop instead of the oval: a scripted full-throttle lap with steer = `−lateral / halfWidth` finishes with `lap` wrapping, no `wall` event on the start straight, and the same state hash on two runs.

## Decisions
_(append dated one-liners as they are made)_
- 2026-09-21 (Adam): **the pier branch is gone**; its ramp is a jump on the harbour side of the main road (`pier-ramp`, lateral 3). A jetty parallel to a 16 m road cannot be faster than the road: sat close, the stay-on-your-road rule never let a kart onto it; pushed 12 m out, its entry kink cost 0.7 s a lap. Side paths help or they go (design §6). Track-builder tests keep a test-only pier (`HARBOUR_WITH_PIER`) as their two-branch example.
- 2026-09-21 (Adam): **side paths help, never hinder** (design §6). Measured solo Hard: the beach (sand, 3.5 m) cost 2.5 s a lap and the pier (3 m, a 20 m bend, longer than the road it bypassed) 3.5 s. Rebuilt: the beach is a boardwalk (road surface, 5.5 m), the pier a straight jetty (5.5 m) with its ramp and a boost pad mid-way. Now beach −1.2 s and pier −0.2 s over a race. ai-driver gate 16 forces a Hard AI onto every shortcut of every track and fails if it is slower than the road.
- 2026-09-21 (Adam, test drive): `nearest()` no longer hands a kart to a shortcut whose centreline happens to be nearer while the kart is still on its own road; it must be past that road's edge less `branchLeaveMargin` (0.85 m) first. Players were being pulled onto the beach by driving near the fork.
- 2026-09-21 (Adam, test drive): a shortcut ribbon drew its kerb stripes across the main road at the fork. Now a branch ribbon loses kerbs and shoulders and sinks 3 cm for `branchBlendMetres` (14) at each end, so the main road draws on top where they overlap (`RibbonOptions.blend`).
- 2026-09-21 (Adam, from the ai-driver session): Harbour Loop turn 1 is now a ~18 m-radius 90° (four control points replace (160, −60)); the kart-controller drift needs a bend at least as tight as `steerRate × driftSteerMin` = 0.84 rad/s, and the old 60–90 m sweepers gave nobody a mini-turbo (ai-driver Lessons). Feature and shortcut `t` values were re-derived from world positions; the file was rewritten by script and validates.
- 2026-09-19: Approach synthesised. Three open questions for Adam before code: shortcut branches need `branch` on the kart state and in `TrackQuery`; the schema has no ground/terrain field; what the `rail` surface does in the kart.
- 2026-09-19 (Adam): Shortcuts are branches. `branch` added to the kart entry in `race-state.schema.json`; `TrackQuery` gains `nearest(position, hint{t,branch}, window) → {t,branch}` and `sample(t, lateral, branch)`. Small kart-controller edit allowed in the build session.
- 2026-09-19 (Adam): `environment.ground {kind: plane|water|none, y}` added to `track.schema.json`. One flat plane per track; Skyline uses `none`.
- 2026-09-19 (Adam): `rail` is a narrow road (halfWidth 3) with normal handling in v1. No new kart code. Rail grinding stays deleted (first-principles §2).
- 2026-09-19: Builder constants live in `track.schema.json` under `builder` defaults; `constants.ts` walks them like kart-controller does.
- 2026-09-19: Bank rise is `−lateral × tan(bank)` (the sign paragraph and test 4 win over the formula line in the model, which had the sign flipped). Normal = `tangent × rightBanked`.
- 2026-09-19: Test 5's "no step larger than 1/2048 of the difference" is impossible with smoothstep over a segment (peak slope 1.5×Δ/segmentSamples). Test asserts ≤ 2×Δ/segmentSamples instead.
- 2026-09-19: `nearestT` may return one sample past the window when the edge sample projects onto its outer segment. Accepted; still local.
- 2026-09-19: Shortcut branches are open centripetal Catmull-Rom (`OpenSpline`, three.js `closed=false` maths) over the authored points only. The first and last authored point must lie within 2 m of the main line at `entryT`/`exitT`; the validator enforces it. One `Lut` class serves both, with a `closed` flag (wrap vs clamp).
- 2026-09-19: Features and hazards may name a `shortcut` (schema field added to jumps, pickups, coins, boostPads). `TrackJump`/`TrackBoostPad` carry `branch`; the kart controller only fires them on the kart's own branch.
- 2026-09-19: Shortcut ends and the start line are baked to world points like features; after a route override their `t` is re-derived with `nearestTGlobal`. A kart on a branch keeps its local `u` through a rebuild; a main-line kart is remapped by position.
- 2026-09-19: `sample(t, lateral, branch)` with `t` outside the branch's range falls through to the main line, so look-aheads past a shortcut exit see the road being rejoined. Needed by the AI driver and by any steering that aims ahead.
- 2026-09-19: `falling` hazards are on the ground and active for `fallingActiveSeconds` at the start of each period; the visible drop is the scene layer's. `gust` is active for the first half of its period and pushes away from its authored side (lateral 0 pushes right). No fall height or duration exists in the schema and none was added.
- 2026-09-19: Test 15 lives in `src/track-builder/__tests__/integration.test.ts` rather than replacing the oval test in kart-controller; the oval tests keep their recorded lap times. The scripted perfect lap is asserted inside the schema `lapTimeWarn` band (40–65 s), not 45–60: a full-throttle ideal line on Harbour Loop is 41 s; humans will be slower.
- 2026-09-19: Harbour Loop authored: 1003 m, 13 points, hairpin ratio 6.15 (guard 1.5). Beach shortcut is sand (`dirt`, hw 3.5, risk narrow), 212 m vs 269 m, about 1.4 s gain at the dirt cap. Pier is hw 3 with a `pier-ramp` jump (launch 5), longer than the main line but a trick chance. The tide closes the beach. Scene layer (`mesh/`), debug overlay and the other five tracks are still to do.
- 2026-09-19: Codex review round one. `sampleInto(t, lateral, branch, out)` added on Lut, Branch, Branches and Track: allocation-free twin of `sample()`; kart-controller and ai-driver hot paths should switch to it (`TrackQuery` still declares `sample()`; the interface change belongs to the kart-controller session). `nearest()` treats a closed hint branch whose range does not contain `hint.t` as main (stale hint guard). Spawn columns now span the full ±0.5 × halfWidth, capped at halfWidth − kartRadius; odd rows inset a quarter step symmetrically; one column alternates sides. Validator rejects non-integer or < 1 `startGrid.rows`/`columns` and non-positive `spacing`. Mesh-layer findings (no `mesh/`, no scene draw-call test) are the next session, not defects.
- 2026-09-19: Scene layer landed under `mesh/` (`palette`, `road`, `chunks`, `decor`, `scene`). `buildTrackScene(track, assets?) → TrackScene { group, chunks, instancers, fog, sky, update(time, active?), drawables(), dispose() }`. Harbour Loop: 25 drawables of the 40 budget, 11 chunks, 48 k ribbon verts. Deviations from the Approach text, on purpose: (a) a shortcut gets `round(CHUNK_COUNT × branchLength / mainLength)` chunks (≥ 1), not 8, so two shortcuts cost 3 chunks not 16; (b) a route override rebuilds every main chunk and the barriers (every LUT sample moves when the length changes), a surface-only shift rebuilds just the touched chunks, branch chunks never rebuild; (c) closed shortcuts (lap gating or the shift) hide their chunks, posts, balloons and coins; `update()` notices an open-mask change so `setLap` needs no event; (d) barrier posts inside another branch's road + kerb are skipped so shortcut mouths stay open; (e) hazard instancers are `frustumCulled = false` and dynamic-draw, moved by `update(time)`; race-manager should pass its own `activeHazards` list in to avoid computing it twice; (f) sky is `MeshBasicMaterial` (a toon dome shades against the sun); (g) the landmark sits at the loop's bounding-box centre on the ground; author a position later if a track needs it. Not done and not claimed: road stripe texture on the UVs, a real water shader, the debug overlay, the `renderer.info.render.calls` assertion (needs a headless GL context; `drawables()` counts visible meshes and non-empty instancers as the proxy), and the other five tracks. Rejected from review: allocation-free `sampleInto` in decor/barrier placement (build time only).
- 2026-09-23: Meadow Run and Canyon Rush added (authored by helper sessions, gated here by the full suite, 401 green): Meadow has a 355 m slipstream straight, a 180° hairpin, a mud stretch, the `hedgerow-cut` shortcut and a storm shift that closes it with grip ×0.85; Canyon has two jumps, the `mine-tunnel` shortcut, and a `collapse` shift whose `routeOverrides` replace the rope-bridge range with the tunnel. Hard AI solo laps land near 40–41 s, the same as Harbour Loop's measured lap, below design's 45–60 s nominal (which the validator's length estimate does meet). The Sunrise Cup and Coastline Knockout now play three different tracks.
- 2026-09-23: Frostbite Pass and Skyline Circuit added (helper sessions; full suite green here, 417). Frostbite: ice stretches, a ski jump, and a `lake-crossing` shortcut closed by `openOnLaps: [3]` and named in `opensShortcuts`, so the frozen lake opens exactly when the blizzard shift fires. Skyline: ground `none`, a climb to 94 m, an `island-hop` jump, and a narrow `sky-rail` shortcut that the sunset shift makes mandatory by splicing its geometry into the main line with `routeOverrides`; no AI fell in 10 extra races. Hard AI solo laps 42–44 s.

- 2026-09-23: Boardwalk Nights added: a narrow arcade-alley shortcut across the chicane (about 1.4 s faster per race for a Hard AI), bumper cars and teacups as hazards, and the fireworks shift adds a ramp at the Ferris wheel on the last lap only. Hard AI solo lap 45.6 s.
- 2026-09-23: Skyline Circuit gets a far-band cloud sea under the road, so the sky islands float on cloud instead of over nothing.
- 2026-09-23: Each biome has its own road and curb colors (palette.ts BIOMES), and the boardwalk road is planks (a 1×64 shade strip on the shared road material). Harbour Loop shows as "Harbor Loop" to players (the user is in the USA); the id stays `harbour-loop` so saved scores match.

- 2026-09-23: Sea tracks get a **coast** (mesh/land.ts): a heightfield over the track box, each vertex at the nearest road sample's height minus the shoulder drop out to 14 m past the shoulder (the roadside decor band), then an eased 12 m slope under the sea; the smooth coastline is where that slope meets the water plane. The art-pipeline gives it grass (Harbor) or planks (Boardwalk) on top and pale sand down the slope, mixed by a per-vertex `blend`. Far decor and the landmark may stand on a wooden **pier** (`decor[].footing: 'pier'`, `environment.landmarkFooting`), raised `pierLift` 1.1 m. The water tracks never cross themselves (the nearest separate sections are 67 m apart), so one height per point is safe; a future track with a bridge over itself needs a rule here first.

- 2026-09-23: Course creatures are a hazard type (`creature` + `creature` kind), scripted in creatures.ts from race time only (deterministic like every hazard). `hazards(time)` gives what can hit now; `pose(time)` gives the scene the creature's place, action and warning marks. A shock wave is an ActiveHazard with `ground: true`: an airborne kart passes over it. race-manager fires one `creature` event when an action changes (sound, dust, camera shake). The AI dodges creature hazards like any other by their radius; the shortcut gate test runs without creatures (their timing is luck, not path).

- 2026-09-23: **Track thrills (design.md).** Ramps are real wedges across the road (`rampRun` 5 m, `rampRise` 0.8 m) and trick bumps are mounds (`shape: "hump"`, `humpRun` 8, `humpRise` 1, rounded off over `humpEdge` 1.6 m at each kerb); karts drive on the same profile (kart-controller `jumpLift`), so nothing clips. Every track has 5 or 6 boost pads: a chain of 3 into its big ramp, one on most shortcuts, the rest on straights; pads glow and scroll (ramps.ts shader). Bump rows: dunes (Canyon 0.92), moguls into the ski jump (Frostbite 0.335), turf humps (Meadow 0.84). No pad in the Canyon mine tunnel: it pushed the Normal AI spread past 20 s.
- 2026-09-23: **Open edges and the claw.** `openEdges` on the def removes the wall on one or both sides; past kerb + shoulder there is no ground (`overCliff`), the kart falls, and race-manager's claw carries it back (2.4 s). The land drops sheer at an open edge (land.ts `CLIFF`).

- 2026-09-23: **Road edges are themed per track** (Adam chose option 1): EDGES in scene.ts picks stripes everywhere, stripes where the road bends (the ribbon's `bend` attribute, radius under about 40 to 80 m), or never, plus two tones, seams and an optional neon line. Barrier posts match (ranch posts on Canyon, snow poles on Frostbite).

- 2026-09-23: **Off-road and real boundaries** (Adam): `offroad: true` on a track makes the LUT sample report dirt past the curb (with the shoulder drop) and `TrackSample.wall` = halfWidth + kerbWidth + shoulderWidth; the kart and projectiles stop there, and boundary.ts draws a continuous hedge, sandstone wall, snowbank or sea wall there. Tracks without it keep the wall at the road edge and get a continuous rail through the posts.

- 2026-09-23: **No posts, strips or walls along roads** (Adam: the Mario Kart World edge). Off-road tracks: the land meets the curb (land.ts offroad: offroadDrop under the road), the ribbon's walled shoulders are discarded, the course limit (TrackSample.wall) is offroadReach past the curb and the roadside scenery starts past it (decorBands.roadsideOffroad). Piers and sky roads: a solid low edge (boundary.ts). A shader override of a vertex-coloured strip goes after `#include <color_fragment>`, or the vertex colours tint it (the curbs showed the old stripes through the new colour).

- 2026-09-23: **One land, drawn and driven** (review: karts snapped 1-11 m at shortcut mouths). terrain.ts RoadIndex: every road piece near a point gives its own land (level from its curb out, offroadDrop under it) and the pieces blend by inverse distance past each curb, so the land meets every curb and runs as one slope between two roads. land.ts draws it and lut.ts drives on it (past the curb, eased over 0.5 m); a shortcut's say fades out over its last 15 m (its ends lie on the main road). The ground plane sits under the main line's lowest curb (groundPlaneY), so a banked corner's low edge no longer sinks into the grass (Meadow's hairpin); scenery stands on the land as drawn.
- 2026-09-23: **Shortcut ends are welded to the main road** (branches.ts weldEnds): where the ribbons overlap the shortcut takes the main road's surface (height, and its bank and climb seen along the shortcut's own lateral, so a road leaving at 70 degrees matches too), then eases back to its own line over 30 m. A kart off its road goes to the road whose edge it is furthest inside (3D distance less half-width), by the 1 m hysteresis. On an off-road track a shortcut's blended ends draw no shoulder and no lane lines (they were the grey slabs in the grass).

- 2026-09-23: **Tunnels** (a shortcut's `tunnel` {from, to}): the Canyon mine ran under the ground sheet (the sky showed through, a kart could leave sideways into the rock). Now its road stays above the canyon floor and climbs inside a mesa: tunnel.ts lays the line, covers any LUT sample on it (walls at hw + kerb, no off-road; the main line too after the final-lap route change), narrows the off-road before each portal (tunnelFunnel) and lifts the land over it (landAbove: the mesa, a ridge with tunnelMesaTop of flat top, a cliff at each portal); the land keeps out of the bore (bore). mesh/tunnel.ts draws the bore, frames, lanterns, portals and the cliff face (vertex colours are linear: convert sRGB picks, or rock comes out pale peach).
- 2026-09-23: Off-road review fixes: creatures hit wherever a kart can reach (the crab waiting on the sand is solid, the Rumblesaur's ring reaches the sand); the start arch's posts stand past the course limit; roadside props stand wholly past it (their footprint too), and every prop keeps clear of all drivable sand; ramps are skirted (ramps.ts, the same slope the karts drive on).
- 2026-09-24: Bug hunt, hand-overs between roads (branches.ts): past a shortcut's open end the shortcut is not there (`nearestLocal` gives no distance once the kart is beyond the end u was clamped to), so out of Canyon's mine a kart is on the main road at once instead of riding a flat extension of the mine's last sample up to 4 m over the sand; and a switch settles its t under the kart (`Branch.settle`, the local search run again until t stops moving), because the first search was centred on the old road's t and could read the new road's ground 12 m away (a 1.2 m snap up then a fall beside the mine entry).
- 2026-09-24: TrackScene.dispose frees everything the scene made: the ground plane, the coast, the sky dome and both kinds of pier are now OWNED, and a mesh marked `userData.ownMap` (the Boardwalk city) frees its texture. Each race left about 1.3-1.6 MB of geometry behind, so memory grew for as long as the game ran.
- 2026-09-24: Time Trial `medalTimesMs` re-measured on all six tracks (they dated from each track's first commit, about 3 × targetLapSeconds, and every run took gold with 20-45 s to spare). From the AI's solo runs at 150cc (Boulder, heavy: 109-120 s): gold about 7 % over that time, silver 15 %, bronze 30 %, in whole seconds. The AI's Pip (light) now earns silver or bronze. game/timetrial.e2e.test.ts fails when the physics, the AI or a track moves the times enough to need a re-measure.
- 2026-09-24: Bug hunt 2, `medalTimesMs` re-measured after the archetype speeds went to −1 % / +1 % (kart-controller Decisions): Boulder's pace was no longer the fastest line and Pip's was no longer short of gold. Gold is now about 6 % over the fastest class's AI time (the mean of its racers' solo runs at 150cc, 116.8-128.7 s), silver 15 %, bronze 30 %, in whole seconds; every racer's AI earns gold. Side effect seen in game/creatures.e2e.test.ts: with the pack level the Rumblesaur and the Yeti catch nobody in about half their races (Rumblesaur 16 catches in 12 races, was 33), so that test now runs four seeds.
- 2026-09-24: Bug hunt 2, shortcut welds (branches.ts weldEnds): the nearest-main-sample walk moved its window while scanning it and read the main road up to 4 samples off, so a welded end sawed from sample to sample (±0.45 m out of Canyon's mine, second difference 0.86 m; Skyline 0.56, Frostbite 0.15, Harbor 0.13). The window now holds still, and the height is carried along from that sample to beside the shortcut's centre by the main road's climb; every shortcut's second difference is under 0.07 m.
- 2026-09-24: Bug hunt 2, the final-lap road (shift.ts weldToRoads): a route override rebuilt the main line from the shortcut's bare control points, but the mine, its mesa, the land and a kart still on the closed shortcut sit on the shortcut's welded ends, so the new road ran up to 3.4 m under them (Canyon: the cover ended 8 m short of the exit portal and a kart hugging the wall was snapped 12 m up onto the mesa; Skyline: a kart finishing the sky-rail drove 3.4 m over the drawn road). Now every new-main sample over a road that was there (the old main line or a shortcut) takes that road's surface under its centre (weldEnds' rule) and eases back to its own line over WELD_EASE where it leaves them; the Canyon mine is covered portal to portal, curb-to-land steps on its final lap are under 0.6 m (were 11.9), and a kart on the closed rail rides within 0.04 m of the drawn road.
- 2026-09-24: Bug hunt 2, creatures and the route change: Canyon's Rumblesaur was authored at t 0.58, inside the collapse's replaced range (0.32-0.665), so on the final lap it stood buried in the mesa and its ring spun karts in the mine. It now stands on the canyon floor at t 0.8 (right side, at the foot of the descent from the outcrop ramp; its ring still rolls curb to curb; at 0.83 its spins sent two AI onto the dune sand and broke the ai-driver dunes gate), a creature keeps its world spot through a route change (keeping its t had moved it 22 m), and applyFinalLapShift switches off any creature on replaced road, as it drops open edges and loops there; a switched-off creature has no pose (no roar) and CreatureView hides it.
- 2026-09-24: Bug hunt 2, big creatures clear the sand: on an off-road track the Rumblesaur and the yeti stood inside the drivable band (hw + 9 and hw + 11 against a course limit of hw + 13), and karts drove through the dinosaur's legs and the yeti's rock ledge. Each now stands `footprint` past the course limit (Rumblesaur 5 m, yeti 7 m: its ledge's reach at the ground); the stomping foot, its ring and the snowball's landing stay where they were, so the hazard is unchanged. Walled tracks are unchanged.
- 2026-09-24: Bug hunt 2, creatures on the ground: the goose, the crab and the Rumblesaur (body, foot and ring) stood at the road centre's height, so the goose waited 0.96 m over Meadow's grass and the crab 0.2 m over the sand; they now stand on the ground under them (the main line's sample at their lateral: the road, or past the curb the land as drawn), and the crab's and goose's hit point with them. The yeti's ledge stands 3 m over the ground under it. The kraken (in the sea), the whale (in the sky) and the snowball (on the road) are unchanged.
- 2026-09-24: Bug hunt 2, the falling rock is seen falling: the 2026-09-19 note left the drop to the scene, which only drew active hazards, so Canyon's rock blinked onto the road for 0.5 s every 5 s and slowed a kart on the tick it appeared. hazards.ts `fallingPhase` (like ventPhase) gives drop / down / idle; for `fallingWarnSeconds` (1 s, new schema default) before it lands the scene draws it dropping from `fallingHeight` (14 m, new) onto its spot, gathering speed, with a shadow growing there (one instanced mesh, drawn only during a drop). The hit window is unchanged.
- 2026-09-24: Bug hunt 3, the land under a road that can go (mesh/land.ts): the land was never built under any road, so a road the race hides (Frostbite's lake crossing on laps 1-2; Meadow's, Harbor's and Canyon's shortcuts and Canyon's old road after the shift) left a road-shaped hole down to the ground (34 m deep on Canyon) while the kart's land (terrain.ts) ran on under it. `hideableRoads` marks those roads (a shortcut with `openOnLaps` or in `closesShortcuts`; the main road a route override replaces, but not its open edges, so the bridge still leaves its chasm) and the land is drawn under them, `offroadDrop` down, as the kart's is. Under a road that is always drawn it is still left out, and it never stands above one. In front of each mine portal the land was cut away 1-5 m out and up to 4 m past the curb (a 24 m drop at the exit): it now stays at the curb, the mesa's foot inside the portal comes down to the road's level, and only a triangle joining that low land to the mesa over the road (a sheet across the mouth) is dropped. The proposed fix (pull down and keep every cell) put land across both mouths; mesh/land.test.ts checks the holes, the bore and the view into it.
- 2026-09-24: Bug hunt 3, boost pads lie on the road (mesh/scene.ts featureMatrices): a pad was a flat panel turned by yaw alone, so on Canyon's 25 % descent into its ramp one end stood 0.53 m over the road and the other sank 0.47 m into it, every lap (Skyline's steep pad and the banked pads on Meadow and Frostbite too). Its frame now comes from the road under it: back to front along its own lane (a changing bank twists the road there, so the centre line's tangent alone was still 12 cm off on Harbor), side to side across the bank, raised just clear of the road at its corners, edges and middle; every pad on the six tracks now sits 3-9 cm over the road.
- 2026-09-24: Bug hunt 3, props stand on the ground (mesh/decor.ts placeDecor): a roadside prop's side was never checked for an open edge (placeBarriers checks it), and a prop was grounded at its centre alone, so beside open edges stalls and lamps hovered over the water off Boardwalk's pier, houses, palms and lamps stood in Harbor's sea, a palm stood on a cliff's lip, and props hung off slopes (Canyon's 17 m mesa had 24 m of air under part of it; chalets and a minecart floated too). A roadside prop is now rejected where its side is open within its footprint plus 3 m along the road, and any prop where the land as drawn rises or falls more than 0.5 m under its footprint; it is placed again elsewhere, and every entry still places all its instances.

- 2026-09-24 (detail review): `fadeNearCamera` (mesh/glow.ts) takes any built-in material and a fade distance (props keep 2.6 m), carries its own view-space varying so an unlit flame takes it too, and patches a material once however often it is handed in: game/ fades rival karts, their flames and the items with it.
- 2026-09-24 (seam review): step 2 of the shift, a kart on a shortcut the new main road now runs under (within its half-width, 3D: `mainUnder`) is on the main road, at the t found there (Canyon's mine, Skyline's rail from its first third on); elsewhere a branch kart still keeps its local `u` and rides the closed shortcut out. Left on branch 1 in the same bore as karts on branch 0, it could not be hit, targeted or hooked by them. items' reseat uses `mainUnder` for shots and drops.
- 2026-09-24 (detail review): The start lamps count the race down. One round lamp per countdown beat (race-state schema `countdownSteps`, `countdownStepSeconds`), under a visor, as their own unlit instancer ('start-lamps', one draw call) under the gantry: lit red left to right with the HUD's number, all green for one beat on the go, dark after (`startLampsLit`, driven from race time by `TrackScene.update`). Colors are linear with the other channels near zero (`LAMP`), since the old 2.2 red baked into the gantry glowed to five peach squares that never changed.
- 2026-09-24 (detail review 5, the land): a `verge` decor band (schema decorBands.verge [2.5, 11] m past the curb: past a ramp skirt, inside the 12 m course limit) of small ground cover karts drive through, visual only and shadowless, one InstancedMesh per asset (tufts, flowers, bushes on Meadow and Harbor; scrub and pebbles on Canyon; saplings and snow-capped stones on Frostbite), keeping off every road and curb, open edges and tunnel mouths (lut.reach); solid props stay past the limit (Harbor adds crates, beach umbrellas, rope posts there). Roadside and verge groups take a sharp corner's outside 80 % of the time (decor.ts outsideOf). Denser: Canyon cactus 70, rock 70 + 40 far; Frostbite pine 120 + 60 far, snowball 80; Meadow barns 30, small windmills 40; Harbor boats 40. Barrels (lift 1.02), minecarts (0.7) and hay bales (1.0) rest on the ground; a test holds every lift to the model's lowest point. Mud (road.ts mudDepth, `surf`): the ribbon keeps the road under a mud patch and the material paints the mud with an edge that wanders up to 3 m in, puddles and two ruts, and no lane lines; only mud samples change, so a surface shift still rebuilds only its chunks. Meadow humps are straw with a red twine crest (design §6 hay humps). Meadow horizon: near hills hazed 0.12 (was 0.3), plus a hedgerow tree line at 420 m in the same mesh (no extra draw). Measured on the M4 Pro at 2048×1536: 71–76 draw calls, 0.90–0.99 M triangles with the shadow pass, 4.5–7 ms a frame with the GPU flushed; the verge itself is about 80–120 k triangles.
- 2026-09-24: Detail review, far horizon (mesh/backdrop.ts): each column of a ring is baked lit or shaded by the sun's azimuth (`flankLight`: FLANK_LIGHT 0.2 for a flank square to the sun, FACE_LIGHT 0.08 head-on; a shaded flank leans SHADE_LEAN 3 times that toward the layer's cool `shade` colour; never past white). A Final Lap Shift recolours the ring (`recolourBackdrop`: its own colours times the sky tint, hazed toward the new horizon), eased with the dome's fade by the session. Skyline has no ring any more: its sky painting's cloud sea replaced the flat cut-out puffs. Frost's far haze 0.5 to 0.42, near 0.28 to 0.22, so the lit and shaded faces read.
- 2026-09-24: Detail review, pickups at night (mesh/glow.ts `selfLit`, scene.ts): balloons and coins add their own colour times one shared `pickupGlow` uniform as emission; `TrackScene.setPickupGlow` takes the sky's SkyLight.glow (0 by day), `update` eases it at the lights' rate and pulses it ±20 % every 1.6 s. On Boardwalk Nights they read coral, teal and gold instead of navy blobs.
- 2026-09-24: Track review, shortcut joins: no shortcut forks off or merges at a T-junction any more (Canyon 82°/59°, Meadow 73°/75°, Harbor 51°/74°, Frostbite exit 77°, Skyline 62°/45°). Each end is a 10-16 m lead-in at 15-25° off the main tangent, then an arc (R 15-40 m) onto the shortcut's line; joins measure 15-28°. validate.ts warns over 30° (a warning so the synthetic test ovals keep their chord shortcuts; validate.test holds the six tracks to it). The Final Lap Shift routes reuse the same points; entry/exit t unchanged, so lap and checkpoint progress through a shortcut map as before.
- 2026-09-24: Track review, Canyon's mine: forks at 25° off the banked climb, arcs (R 15) into a straight bore (heading -53°), and merges at 15° 16 m before exitT. The bore now holds about 7.5 m from the fork to u 0.4 (it dipped to 0.6 m), then climbs about 22 % to a long crest at 28.6 m, so the final-lap road crests over 40+ m instead of 15 m (karts flew off it and slid onto the sand); portals at u 0.161 and 0.795 (land and bore tests pass there; 0.8 and 0.815 leave a land hole or a 0.13 m curb step).
- 2026-09-24: Track review, final-lap road: shift.ts spliceRoute also drops a kept main point within 2 × the route end's half-width of either end (Skyline kept one 0.16 m past the rail's end: 42° over 2 m, width 9 to 14 m in 1 m, a folded left edge). validate.ts now runs its hairpin check, a width-rate check (≤ 0.25 m per metre) and a control-point spacing check (≥ 4 m) on the main line, every shortcut and the spliced final-lap road; before, only the lap-1 main line was checked.
- 2026-09-24: Track review, shortcut welds (branches.ts weldEnds): past the main road's curb the land is level, so a welded shortcut's copy of the main road's bank fades out as its centre leaves the curb (over its own half-width). A 25° fork off Canyon's banked climb kept the full bank on a curb-level centre and its inner edge sat 0.35 m under the main road at the hand-over.
- 2026-09-24: Track review, respawn onto hazards: validate.ts rejects a fixed hazard (static, vent) 0-10 m past a checkpoint or within its reach plus a kart behind one, on the lap-1 road and the final-lap road. Moved: Boardwalk teacup-2 0.87 → 0.89 (it sat on checkpoint 9), Frostbite steam vents 0.935/0.944 → 0.926/0.935 (steam-2 was 2.9 m before checkpoint 11), Canyon geysers 0.163/0.172 → 0.145/0.154 (the shorter final-lap road put checkpoint 2 on them). No respawn.ts change was needed.
- 2026-09-24: Track review, Frostbite's ski jump: a ramp spans kerb to kerb, so its lateral changes nothing (set to 0 for clarity). The right open edge moved from 0.46-0.58 to 0.535-0.6, past the landing: 0/22 launches from any lane at up to 20° now fall (was 3), and none of the karts that missed the ramp falls before 0.535. kart-controller ramp-review keeps its open-shoulder case on a copy with the old edge.
- 2026-09-24: `medalTimesMs` re-measured on all six tracks after the AI rework (ai-driver Decisions, 24 Sept: drifts that pay, hazard dodging, Hard always takes a wide shortcut), same method: gold about 6 % over the fastest class's AI time (mean of its racers' solo runs at 150cc; 5.9-6.4 %), silver 15 %, bronze 30 %, whole seconds. Harbour 123 / 133 / 151 s (was 126 / 136 / 154), Meadow 124 / 134 / 151 (126 / 137 / 155), Canyon 114 / 124 / 140 (124 / 134 / 152), Frostbite 130 / 141 / 159 (135 / 146 / 165), Boardwalk 125 / 135 / 153 (129 / 140 / 158), Skyline 132 / 143 / 162 (136 / 148 / 167). Open: Canyon's mine is worth 4-6 s a lap to the AI (forced vs forbidden, 150cc and 100cc), over design §6's 2 s.

## Lessons (repair loop writes here)
_(error → cause → fix → rule; newest first)_
- 2026-09-19: **Moving hazards would vanish after frame one (caught in review, not play).** Cause: `InstancedMesh` computes its bounding sphere lazily on the first render from the instances present then, and never again; a rolling barrel leaves the sphere and is culled, and an instancer whose first frame has `count = 0` gets an empty sphere and is culled forever. Fix: `frustumCulled = false` on every instancer whose matrices change at runtime. Rule: anything animated through `setMatrixAt` is never frustum-culled, or its `boundingSphere` is reset to `null` after every update.
- 2026-09-19: **Kart circled the beach exit and never rejoined.** Cause: the driver's look-ahead `sample(t + 0.02, 0, branch)` clamped to the branch's end point, so the target sat still. Fix: `Branches.sample` falls through to the main line when `t` is outside the branch range. Rule: any query at a `t` beyond a branch's exit means "back on the main line"; never clamp a look-ahead.
- 2026-09-19: **Kart could not enter the beach shortcut.** Cause: the main road's wall clamps a kart to `halfWidth − kartRadius`, and `nearest` only switches once the branch centreline is more than 1 m closer than the main one; a branch that diverges fast is out of reach before the kart gets to the edge. Fix: none in code; the test driver hugs the inside edge from 0.04 lap before the entry. Rule: author shortcut entries to diverge from the main line at ≤ 30° for the first 20 m so the branch centreline is inside the main road envelope where a kart can reach it.
- 2026-09-23: **After Canyon Rush's bridge collapse, the mine tunnel lost its walls (bug review).** Cause: open edges were laid by t, and the route override gives old t values to new road. Fix: open edges and loop feet keep world points and are re-derived after a route change; those on replaced road are dropped; the validator rejects an open edge that only partly overlaps a route override. Rule: anything placed by t that must survive a route change stores a world point, like shortcut ends.
- 2026-09-23: **Ramp, bump and loop geometry leaked on every rebuild and race.** Fix: the scene adds them to `OWNED`. Rule: every mesh the scene builds itself is registered for disposal where it is made.
