# SOP — VFX and juice

## Purpose
Drift sparks tiers, boost flames and FOV kick, chromatic aberration, speed lines, screen shake, tyre marks, dust, hit reactions, pickup pop, position flourish, lap banner, confetti, idle life.

## Inputs
Race events.

## Outputs
Particles, post-FX parameters, camera modifiers.

## Constraints
Trauma-based shake with rotation; hit-stop 60–90 ms; all effects gated by reduced-motion; post-FX in one EffectPass; N8AO High only.

## Tests (must pass before merge)
Frame time under 12 ms on Low with all effects firing at once in a stress scene; the 12-item checklist in research plan §7.2 all ticked.

## References
Juice It or Lose It; Art of Screenshake; pmndrs postprocessing.

## Approach
_Written 23 Sept 2026 from research plan §4.7 and §7.2 (the 12-item juice checklist), design §7, the kart-controller events (`driftTierUp`, `boostStart`, `hit`, `landed`, `wall`, `bump`), the race and item events, and the pmndrs postprocessing API._

**Pure layer (tested):**
- `trauma.ts` — trauma in [0, 1], added by events (hit 0.5, landing 0.2, boost 0.15, wall 0.25), decaying 1.6 per second; shake = trauma², offsets from a seeded smooth noise, **rotation a fraction of a degree** (max 0.6°), translation max 0.35 m.
- `camera.ts` — FOV kick on boost: +12° in 0.12 s, eased back over 0.6 s; −5° on a hit for 0.25 s; roll ±3° with drift direction.
- `director.ts` — events → effect cues: spark tier and colour, bursts (balloon pop, coin, hit stars, confetti), skid on/off per kart, hit-stop (75 ms, player only), finish slow-motion (0.3× for 1 s, player only).
- `timing.ts` — the sim-time scale the loop applies to the accumulator (hit-stop 0, slow-mo 0.3, else 1). The sim stays deterministic: only how many fixed ticks run per real second changes.

**Three layer (smoke-tested):**
- `particles.ts` — one `InstancedMesh` of camera-facing quads (billboard in the vertex shader), up to 2,048 live, per-instance colour and life, CPU integration with gravity and drag, additive blending. One draw call.
- `skids.ts` — a ring buffer of tyre-mark quads laid while drifting, multiplying the road darker and fading over 4 s (`SKID`). One draw call.
- `speedLines.ts` — thin streaks in camera space while boosting. One draw call.
- `post.ts` — one `EffectPass`: bloom (high threshold, so only sparks, flames and lamps glow), chromatic aberration (boost only), vignette, ACES tone mapping. Off on `quality: low`.

**Reduced motion** (ui-hud setting or OS): no shake, no speed lines, no chromatic aberration, no hit-stop, no slow-motion; FOV kick halved.

**Tests:** trauma decays to zero and never exceeds 1; shake rotation stays under 0.6°; FOV kick timing; director mapping per event; timing scale per state; reduced motion zeroes the right things; particle pool never exceeds capacity and recycles oldest first.

## Decisions
_(append dated one-liners as they are made)_
- 2026-09-23: Built. `src/vfx-juice/` = `juice` (pure: trauma, camera kick, drift roll, time scale, event director, spark colours; 8 tests), `particles` (one pool, one draw call each: additive glow, soft dust, square confetti), `trails` (tyre marks ring buffer, camera-space speed lines), `post` (bloom at HDR threshold 1.0, chromatic aberration on boost only, vignette, ACES in one EffectPass), `vfx` (emitters and bursts).
- 2026-09-23: Checklist status (plan §7.2): 1 sparks 3 tiers ✓ (rumble later); 2 boost flame, bloom, FOV kick, chromatic, speed lines ✓; 3 trauma shake with sub-degree roll ✓; 4 drift roll ✓ (spring camera was already there); 5 tyre marks fading 10 s ✓; 6 off-road dust ✓ (idle exhaust not done); 7 hit-stop 75 ms, FOV −5°, hit stars ✓ (squash-stretch and dizzy stars not done); 8 balloon pop shards ✓; 9 position flourish ✓ (ui-hud); 10 final-lap banner ✓ (ui-hud); 11 confetti and 0.3× slow-mo ✓ (camera orbit not done); 12 idle life not done (merged meshes).
- 2026-09-23: The time scale changes how many fixed ticks run per real second, never the tick itself, so hit-stop and slow-mo keep the sim deterministic.
- 2026-09-23: Measured: 71 draw calls for a whole frame with the shadow pass and the post chain (`renderer.info.autoReset` off so the count covers every pass). The SOP's 12 ms stress-scene gate still needs a visible browser; the test pane is hidden and throttled to about 1 fps.
- 2026-09-23: Critique (Codex `gpt-5.5`), both accepted: `Vfx.reset()` now also clears the time scale and camera kicks, so a restart mid hit-stop or slow-mo never starts frozen; emitters reuse constant colour tuples and a scratch spark colour instead of per-particle arrays.
- 2026-09-23: Boost flames sit on the pipes (flames.ts): one additive mesh per pipe on the chassis, shown only while boosting, flickering, longer with more boost left; embers stream off the same pipes in the racer's color. Reduced motion holds the flame steady.
- 2026-09-24: The chase camera (game/camera.ts clampToRoad, after smoothing) keeps `roadClear` 1.2 m over the ground under its own spot, and where that road is a tunnel's (covered or bore) stays `beamClear` 0.45 m under tunnelWall, below the timber beams. The pose rode the kart's height: on the Canyon mine's exit climb, look-back put the camera under the road (the screen went sand) and driving forward it rose past the beams, which hid the kart. Elsewhere it only lifts a camera about to touch the road (Skyline's steepest drop); four tracks are untouched.
- 2026-09-24: A bumper car's shove and a rockfall's slow jolt the player like a wall (`traumaWall` and the wall dust) from their `hazardHit` race event. They raise no kart event, so they used to land with no shake and no dust (a spin hazard shakes through its kart `hit`).
- 2026-09-24 (detail review): The chase camera frames your kart big in the lower third at every speed (plan §4.7): 5.5 m back (+0.5 at top speed, was 6.3 + 1.8), 2.4 m up so it sees past a 2.2 m racer's head, looking 6 m ahead at 1.5 m; field of view 60° + 6° at speed, and the boost kick capped at `fovMax` 74° (was 66 + 12 + 12 = 90°). It rides with the kart (`carry`) and smooths only its offset, so speed no longer trails it v/lag m behind; yaw lag, look-back, the loop side view and the road/beam clamp are unchanged. A rival's kart and its boost flames dissolve within `kartFade` 4 m of the lens and items within `nearFade` 2.8 m (glow.ts fadeNearCamera, the props' dither); your own kart gets unpatched material copies, and 2.8 m keeps what it trails (items share one mesh per kind) clear (camera.test.ts).
- 2026-09-24 (detail review): Particles never fill the screen: every pool fades out between 1.2 and 3.5 m of view depth and draws no bigger than `PARTICLE.maxSize` metres per metre of depth (confetti 0.04, dust 0.3, sparks 0.12); dust and smoke are soft puffs with no hard rim. Confetti is 2:1 paper strips that turn and flip (width through zero, shaded as it turns) in saturated hues (`CONFETTI`, no white or pastels). The player's finish shower falls 4 m plus 0.4 s of speed up the road (`CONFETTI_BURST`) and the STRIKE confetti is thrown forward and out (`STRIKE_BURST`), so neither lands in the chase camera.
- 2026-09-24 (detail review): Tyre marks multiply the road (Zero, SrcColor blending) by 0.55 fading to 1 over 4 s (`SKID`), so rubber darkens any deck; the old fixed grey over normal blending read as lane paint on Boardwalk's night boards.
- 2026-09-24: Detail review: the post chain's saturation lift follows the sky (`Post.gradeTo` = SkyLight.grade, eased like the lights, snapped at a new race): 0.03 under Canyon's dusk, 0.06 under Skyline's night, 0.12 elsewhere.
- 2026-09-24: Exhaust flames burn from the kart's own `userData.exhaust` when it has one (art-pipeline kart.ts: a Classic or Buggy body's pipes), else from the racer's `EXHAUST`; the flame colour stays the racer's.
- 2026-09-24 (camera and speed; four of five video critiques: "rigid chase camera and a weak sense of speed; boosts give little feedback (no FOV punch, no wind lines, no shake)"): each boost punches by its kind (juice.ts `JUICE.punch`, from the kart's `boostStart` source and length, and from the player's Fizz Pop and Grapple slingshot item events, whose kart events the items step keeps to itself): +5, +7.5, +10° of view for a blue, orange, purple mini-turbo, trick +6.5, rocket start +8, pad +8.5, item +9.5, slipstream +3.5, in 0.1 s and eased home over 0.65 s, with the camera falling back 0.2 to 0.6 m and a short, low shake (trauma 0.14 to 0.36: under 5 cm). While a boost runs the view holds up to +4° and 0.3 m back (`holdFov`, `holdBack`, by its strength, eased). `CAM.fovMax` 74 → 80; at the purple punch's peak at top speed your kart is still about a tenth of the screen wide, and a boost's hold keeps it a ninth (camera.test.ts). A first round (+4 to +8°, fovMax 76) still read as "an extremely weak FOV punch" to the reviewer.
- 2026-09-24: The speed lines drew at last (see Lessons): 72 streaks in screen units (5.2 px wide at 1280x720, 7.8 at 1920x1080), on an ellipse fitted to the screen from 0.74 to 1.2 of the way to its edge, none in a 40° cone straight down the screen (your kart and its road), normal blending (additive white vanished against a bright sky), alpha 0.62; a weak boost shows 45% of them. They and the post chain's lens run on `Vfx.boostLevel` (the boost's hold, or its punch while stronger; 0 under reduced motion).
- 2026-09-24: The boost lens (post.ts `LENS`) takes the EffectPass's one convolution slot from the plain chromatic aberration: the screen's edges streak toward its middle (8 dithered taps, masked to an ellipse from 0.62 to 1.17 of the way out, so the road ahead and your kart stay sharp) with a small red and blue fringe. Cost at 1920x1080 on the M4 Pro (muted headless Chrome): whole frames with it on against off, +0.14 ms median, inside the noise of a shared machine; the pass alone in a microbenchmark, not measurable against a plain copy (a 64-tap version: +0.02 to 0.05 ms). Under the 0.5 ms allowed.
- 2026-09-24: The drift roll eases in and out (`DriftRoll`, 5/s, full from 12 m/s); the 3° snap on a drift's start and end read as a glitch. Reduced motion keeps 40% of every widening and pull-back (`reducedKick`) and half the look into bends; no shake, roll, streaks or lens.
- 2026-09-24: The chase camera moved out of main.ts into game/camera.ts `ChaseCam` (tested headless through whole races in camera.e2e.test.ts). New: the field of view at top speed +8° (was +6); the height it rides at follows the kart's at 8/s, so a drift's hop or a bump no longer bounces the whole view and off a ramp the kart rises in the frame first ("rigidly mimics the chassis's vertical jitter"); the surge (0.08 m back per m/s the kart gains on the camera's own speed, eased at 3/s, at most 0.45 m; the same closer when it brakes); it looks into the bend ahead (half the road's turn 0.6 s of speed up it, 8 to 18 m, at most 16°, eased at 3/s) and swings wide of it (up to 0.7 m to the outside, 0.6 m more in a drift, at most 1.1 m, eased at 2.5/s: "rigidly locked to the kart's rear axis in the drift"). The loop side view is a smoothstep blend in time, 1.1 s in and 1.25 s out, with the chase rig kept live so the swing back lands on it, and held after the ride until the kart is 9 m past the ring so the swing crosses ahead of the ring, never through its rising side; it stands 2 ring radii out on the side the karts ride in on (was 2.4, always the left: in Mirror mode the swing crossed the whole ring's width and turned 5.4° a frame), turns 70% of the way toward the kart and slides 20% with it, a camera that follows the kart round rather than a still; its move across the road is done by 70% of the blend (`loopAcross`), so it crosses the gantry posts' line well behind them on the way out and well past them on the way back (through races on Boardwalk and its mirror: at most 1.14 m and 3.5° a frame, never nearer than 2.7 m to the ring or its gantries). It was an exponential ease in and the chase lag out: 15.6 m and 26° in one frame on the way out (the wall clamp pulled the side view in), Boardwalk's "static loop camera cut".
- 2026-09-24: Falls (Boardwalk's "jarring underwater transition"): the camera followed a kart off the pier 5 m down to where the claw caught it and aimed 2.4 m under the sea. Now it follows a fall at most 1.5 m (`fallFollow`), never lower than 0.3 m over the water for its aim and 1.2 m for the lens (`seaFollow`, `seaClear`), and while the claw carries the kart it watches from the road (at most `carryRise` 1.8 m over where it fell), inside the open edges too, finding the road under itself (the kart's own place on the track stands still in the claw): it had ridden up through the start gantry's beam. The followed height eases (6/s) into and out of all this, never a jump.
- 2026-09-24: The mine's light (Canyon's "abrupt, flat tunnel lighting"): main.ts dims the day's light by the camera's depth in the bore (`ChaseCam.tunnel`: from 4 m outside a portal to 10 m inside, eased at 2.5/s): sky and ambient to 60 and 62%, warmed 40% toward lantern light, the sun (shadowed in there anyway) to 85%, so the lanterns glow and the fade runs over about a second instead of the sun's shadow edge snapping it.
- 2026-09-24 (VFX polish; video critiques: "opaque & cluttered VFX cones", "cheap-looking cyan exhaust flames", drift sparks "blinding visual clutter" on Skyline and "low quality" elsewhere, skid marks "harsh, flat decals", rivals near the lens "a coarse stipple"): boost flames are compact jets (art-pipeline flames.ts): one mesh a kart holds a jet on each pipe (one draw call a boosting kart, was one a pipe), a soft teardrop with a white-hot core and soft edges, premultiplied blending so its color holds over a bright sky (additive washed it pale). A mini-turbo burns its tier's color (`TIER_RGB` blue, orange, purple; `BoostTier` reads the tier the drift let go at, else the boost's length), any other boost the racer's own (design §5). `FLAME`: 0.46 / 0.6 / 0.76 m by tier, 0.7 an item, pad or trick, 0.44 a slipstream, half the racer's exhaust `size` counting; 35% longer as it fires, shrinking over its last 0.3 s; flicker and a ripple down the jet, steady under reduced motion. A rival's fade with its kart near the lens (they share its ghost's opacity, `uKart`), smoothly, no dither.
- 2026-09-24: Drift sparks (kartfx.ts `SPARK`, one streak pool, one draw call): crisp streaks, not discs (particles.ts `streaks`: a capsule over 0.09 s of the spark's own motion, a white-hot core line inside the tier's color, brightest at its head, bright enough to bloom), 50 / 58 / 66 a second by tier (was 70, living up to 0.45 s against 0.32) and none while a drift charges, a rival's 55%; each keeps 84% of its kart's velocity, so it falls behind its rear wheel in a short fan thrown out to that wheel's side and up, never ahead of the kart; a glow on each rear wheel in the tier's color reads the tier. Boost embers are streaks in the flame's color, 72% carried: a short trail. At most 29 streaks a kart (`streaksPerKart`). Landing and wall scuffs are 8 small gray puffs (0.3 m, 0.38 s); they were 10 pale ones growing to 1.25 m, "large, blurry circles" behind the kart at night.
- 2026-09-24: Tire marks (trails.ts `SKID`): 0.68 at their darkest (was 0.55, near-black on Boardwalk's deck), 0.24 m wide with soft edges, the rubber laid unevenly along them (world-space noise), held and then eased away over 3.5 s, a drift's first 0.3 s fading in (kartfx.ts `MARK`); a segment per 0.45 m a wheel moves (was one a frame), in a 1,600-quad ring.
- 2026-09-24: Near the lens (game/kartFade.ts `KartFader`): a rival's kart turns into a see-through ghost, not a dither. Each of its meshes gets two hidden copies sharing its geometry and morph targets (the rig's lean and steer): a depth-only copy drawn first, then a see-through copy only the front surface passes, so the ghost is one clean layer. Within CAM.kartFade of the lens (its box's nearest point) the mesh wears a never-drawn material and the copies show; its opacity is the square of the way from 0.8 m to 4 m (`ghostAlpha`). Ghosts draw last in the see-through pass (`GHOST.order`, back to front; every Group in the kart carries it, as three takes a mesh's group order from its nearest Group), after the sparks and flames, so those behind a ghost show through it and its depth copy hides only its own insides. Switched from the scene's onBeforeRender with that frame's camera (a registry: the next race is built before the last is freed); the copies hang in the scene hidden, so the warm-up compiles them; no draw while far. Items (their own copy of the vertex toon, and the Strike Ball) fade by alpha in the see-through pass (glow.ts `fadeNearCameraAlpha`); props keep a dither, now interleaved gradient noise (no 4x4 grid).
- 2026-09-24 (finish celebration and podium ceremony): `directFx(…, confettiFor)`: the finish confetti falls only for a joyful place, as the game's reaction says (a win, a podium place, a safe Knockout place, a Time Trial medal); every finish still gets the slow-mo, which now eases back to full speed over its last `slowMoEase` (0.4 s; the step read as a lurch under the finish camera's swing). The podium's `Vfx.firework` (72 sparks in one confetti hue pushed past 1 so they bloom, and a white heart; 30 with reduced motion; soft and slow, never a strobe) and `Vfx.confettiRain` use the glow and confetti pools: no new draw or shader.
- 2026-09-24 (course intro, game/intro.ts): before the countdown the camera flies each track's authored flight (a banked sweep toward the far landmark, the signature stretch, a truck past the grandstand laid from the stand as built, coming to rest on its crowd, a crane down behind the player's kart), each move on a sine-ramped ease along an arc-length Catmull-Rom rail, cuts between; the low moves are laid over the road (clampToRoad plus 0.4 m, lifted whole to pass 3.5 m from any balloon's middle). The crane ends on `restPose()`, and `ChaseCam.reset(k, rest)` starts the chase camera there, so the hand-over does not move. The course's creatures and hazards are drawn at the intro's scene time (`RaceSession.frame(…, sceneTime)`), which runs into the sim's own at tick 0. The sun's shadow box centres 40 m out in front of the lens, not on a landmark 500 m off. intro.e2e.test.ts sweeps every frame against the built scene, the karts, the creatures and the balloons, mirrored too.
- 2026-09-25 (Final Lap Shift set pieces): `directFx` turns `trackChanged` into `shiftPulse` (CameraKick.pulse: +5° of view and 0.35 m back, up in 0.12 s and eased back over 0.9 s; not a boost, so no speed lines or lens) and 0.3 trauma, none of it with reduced motion. `Vfx.shiftBurst` throws the set pieces' bursts (track-builder mesh/shiftStage.ts): dust (a big puff near the player's kart shakes the camera a little), a plume out of the canyon, sparks, an oak's leaves, and fireworks three times the podium's size high over the road. main.ts flashes the lights with the storm's lightning (`stage.flash`: sky light up to 2.8 times, ambient 3.4 times at the peak; a third as bright with reduced motion).
- 2026-09-25 (finish celebration): the finish camera (4.4 m off your kart, 48° of view) flew through the balloon row past the line, and one balloon filled a quarter of the frame as a giant stippled blob (review, Harbor Loop Quick Race win; reproduced at 1.9 s into the celebration). While celebrating, main.ts asks the track scene for its close-up (`TrackScene.lens(camera, true)`): balloons and coins fade as clean ghosts from 6 m and are gone by 2.4 (a race's 2.6 / 0.8; track-builder ghost.ts `PICKUP_GHOST`). The camera's path is unchanged; the creature and the hazards fade near every camera (2.5 / 5.5 m).
- 2026-09-25 (podium ceremony): each place plate is centred on its own block's front, between the band round its foot and the lip of its top, as big as that front allows (podium.ts `platePlace`, `BLOCK`): it was sized and set from the block's whole height, so the short 3rd block's "3" ran under its lip (hidden from the camera above) and its band. The 3rd block is 0.6 m tall (was 0.5: its front showed 0.24 m between the trims, room for a 0.2 m plate; now 0.3 m). podium.test.ts checks every plate against its own front. On the way, the blocks' fronts (turned from the sun on Harbor) streaked with shadow acne, the lip of the 3rd a sawtooth in the close shot: the blocks (the shared vertex toon, or the night one) and the plates take no sun and no shadow lookup on faces turned from it (track-builder glow.ts `sunlessBackFaces`; art-pipeline SOP), and still take the karts' shadows on their tops.

## Lessons (repair loop writes here)
_(error → cause → fix → rule; newest first)_
- 2026-09-25: **The podium's "3" was cut off by its step's top (review still).** Cause: every place plate was sized 0.82 of its block's whole height and centred on the whole block, but a block's front only shows between the 0.14 m band round its foot and the 0.12 m lip of its top; on the 0.5 m 3rd block that is 0.24 m, and the 0.41 m plate ran under both. Fix: `platePlace` fits and centres each plate in the front its block shows. Rule: place a decal by the face it sits on, trims taken off, never by the whole solid's height.
- 2026-09-24: **Firefox drew the grass's dark greens, Pip's teal kart and every balloon black (17 to 38% of the screen on Meadow, Canyon and Boardwalk); Chrome and Safari looked right.** Cause: the grade's HueSaturationEffect clamps only the top, so a strong colour's weakest channel went below 0, and the sRGB encode after the chain took pow() of it: NaN, which Chrome writes as 0 and Firefox as a black pixel. Found by toggling the chain's parts in headless Firefox (grade 0: clean). Fix: a FloorEffect last in the EffectPass, max(rgb, 0); Chrome's pixels identical on all six tracks (read back before and after). Rule: every post chain ends with no channel under 0, and a new effect is looked at in Firefox and WebKit too, not only Chrome.
- 2026-09-24: **The new streak sparks and embers drew nothing.** Cause: the streak shader mapped the quad's x onto the axis turned counter-clockwise, which mirrors it, so every spark was culled as a back face; the program linked and nothing logged. Found by drawing one spark alone into a render target and counting its pixels. Fix: across is the axis turned clockwise, (axis.y, -axis.x); particles.test.ts checks the winding for every axis. Rule: a quad laid out by its own shader keeps the plane's winding (or is DoubleSide), and is checked by its pixels.
- 2026-09-24: The boost speed lines never drew, and "no wind lines" was the critique. Cause: the vertex shader maps the quad's (x, y) onto (across, along) the streak, a mirror image, so every quad faced away from the camera and was back-face culled; no error, nothing on screen. Fix: `side: DoubleSide`. Rule: a mesh placed straight in clip space by its own shader is checked by drawing it alone into a render target and counting its pixels, not by the absence of errors.
