# SOP — Backend and leaderboard

## Purpose
Supabase table + Edge Function that validates and inserts scores, serves leaderboards and ghosts, daily seed.

## Inputs
Score payload, input log.

## Outputs
Leaderboard rows, ghost logs.

## Constraints
RLS on; select open; no anon insert; service-role key only in the function; rate limit per IP; re-simulate input log to verify time; Turnstile optional; keep-alive ping every 6 days.

## Tests (must pass before merge)
Probe script inserts and reads; a forged timeMs is rejected; 20 inserts in a minute from one IP are throttled.

## References
research plan §6.6; Supabase rate-limiting docs.

## Approach
_Written 23 Sept 2026 from research plan §6.6 (and §8 red-team), the score and save schemas, the deterministic sim (race-manager, kart-controller, track-builder sim layer, items: no Three.js, no wall clock) and Supabase's Edge Function and RLS guidance._

**Data** (`supabase/migrations/`): `scores` (name, track_id, mode, daily_seed, speed_class, racer_id, time_ms, lap_times_ms, input_log, client_version, ip_hash, verified, created_at) and `submit_attempts` (ip_hash, at). RLS on both with **no policies**: anon and authenticated can neither read nor write the tables. Reads go through `get_leaderboard(track, mode, seed, limit)`, a `security definer` function with an empty `search_path`, returning only safe columns of verified rows, granted to anon. Ghosts through `get_ghost(id)` the same way.

**Writes** (`supabase/functions/submit-score/`): one Edge Function holding the service-role key (Supabase injects it; it never reaches the client). In order: CORS; JSON shape and field rules (the shared `validate.ts`: name 1–16 of `[A-Za-z0-9 _-]`, a small word filter, known track and racer, mode `timeTrial | daily`, 150cc, integer time ≥ 30 s, log ≤ 256 KB); **rate limit** (salted SHA-256 of the IP, more than 10 attempts in 60 s → 429); **re-simulation** (`verify.ts`, the real sim bundled into the function: decode the input log, run a solo 150cc race on the claimed track and seed, and require the replayed finish time to equal `timeMs` exactly); insert with `verified = true`.

**Replayable input.** The player's input is quantised (`quantize()`: steer, throttle and brake to 1/127 steps) *before* the sim sees it, so the log holds exactly what the sim used. Encoding: 4 bytes per tick (steer int8, throttle, brake, flags), run-length encoded, base64. Time Trial uses seed 0; Daily uses the UTC date seed.

**Daily Challenge** becomes a solo time attack on the day's track at 150cc (a full 8-kart re-simulation would not fit an Edge Function's CPU budget). Time Trial is solo with items off; Daily is solo with balloons live.

**Client** (`src/backend-leaderboard/`): `config.ts` (project URL and the publishable key, which is public by design), `client.ts` (`submitScore`, `fetchLeaderboard`, both failing soft offline), `inputlog.ts`, `validate.ts`, `verify.ts`. The results screen of Time Trial and Daily submits the run and shows the top 10 with the player's row.

**Keep-alive**: a scheduled GitHub Action calls `get_leaderboard` every 6 days so the free project never pauses.

**Tests**: codec round-trip and size; quantise is idempotent; validation rejects every bad field; a scripted solo race's log re-simulates to the same time; a forged `timeMs` (one tick off) is rejected; a tampered log is rejected. Live probe (`scripts/probe-leaderboard.ts`): a real run inserts and reads back; a forged time gets 422; 20 submissions in a minute from one IP get 429 after the tenth.

## Decisions
_(append dated one-liners as they are made)_
- 2026-09-23: Supabase project `rascal-rally` (ref `thuvqdejckcphwuooyhx`, East US, Free plan), created by Adam: Data API on, new tables not auto-exposed, automatic RLS on.
- 2026-09-23: Migrations applied: `scores` and `submit_attempts` with RLS on and no policies; `get_leaderboard` and `get_ghost` are the only public reads; the automatic-RLS helper's EXECUTE revoked from anon and authenticated. Checked: anon cannot select or insert `scores`, can call `get_leaderboard`.
- 2026-09-23: **The replay is the truth.** The stored time is always the server's replay time; the claim must be within `CLAIM_TOLERANCE_MS` (1 s) or the run is rejected. Exact equality would reject honest Safari or Firefox runs if their maths differs from the server's V8 in the last bit.
- 2026-09-23: Daily Challenge is a solo 150cc time attack on the UTC day's track (sorted track ids, seed = YYYYMMDD); yesterday's seed is still accepted so a race started before midnight counts.
- 2026-09-23: The player's input is quantised to 1/127 steps before the sim sees it (`game/simtick.ts`, shared with the server), so the log reproduces the run exactly; −0 is folded to 0.
- 2026-09-23: The function bundles the real sim (`npm run build:function` → `core.js`, generated). **Any change to the sim, the tracks or the kart schema needs `npm run deploy:function`** or honest runs stop verifying; bump `CLIENT_VERSION` when old clients must reload.
- 2026-09-23: Deploy path: the Supabase CLI (`npm run deploy:function`), after Adam logs it in once. Pasting the 176 KB bundle through the MCP deploy call was rejected as error-prone; loading it from a pinned GitHub commit waits on Adam's go-ahead to push.

- 2026-09-23: Red-team fixes: the stored log is canonical (cut at the finish, horn cleared) with a unique index per board, so one drive is one row; get_ghost is revoked (unused, and it let strangers copy the top run); the rate limit is one atomic SQL step (take_submit_slot: advisory lock per client, 10 a minute, 60 accepted runs a day) that fails closed (503); IPv6 counts by its /64; oversized bodies are refused before reading; yesterday's Daily closes 15 minutes after midnight UTC. Migration 20260923000003 is written but NOT applied yet: the live change needs the user's OK. Deploy order: migration first, then the function (the function calls take_submit_slot).
- 2026-09-23: Known limits, by design: a bot that drives well passes verification (it checks physics, not humans), and CORS stays open until the production origin exists. The board is for fun; review stored logs before any prize.
- 2026-09-23: **Deployed** (Adam's OK in chat): submit-score v1, verify_jwt on, through the Supabase connector. The entry imports the published core pinned to a commit on jsDelivr (`node scripts/fn-deploy-entry.mjs`); the bundler refuses a github.io import. Live check: CORS 200, bad body 400, a time 5 s faster than the run 422 with the replay time equal to the local replay (131892 ms), no row saved. After every `npm run build:function` that changes the hash: push, then redeploy with the new entry.
- 2026-09-23: The bundle test also drives two runs on the grass beside the road (Meadow Run, Frostbite Pass): the three on-road runs did not notice an off-road physics change, so a stale bundle would have rejected every run that cut a corner.

- 2026-09-24: Live check: the hardening migration is applied (harden_scores, 20260923190717; the 'NOT applied yet' note above is out of date). Supabase security advisors: RLS on with no policies on scores and submit_attempts (by design: only the function writes, only get_leaderboard reads) and get_leaderboard callable by anon as SECURITY DEFINER (by design: the one public read). Names on the board are set as text (ui-hud h()), never HTML.
- 2026-09-24: Bug hunt 3: submit-score's rank is the name's place on its board, found by name, not by this run's id: get_leaderboard keeps each name's best run, so a slower second run under the same name (or a common name someone else holds) came back rank null, "outside the top 50", while the board showed the name 1st. The reply adds `bestId` and `bestMs` (the name's run on the board); the client hands them on as `best`, and an older function without them counts as the run itself. The entry is tested headless (`submit.test.ts`: Deno and the database stubbed). Needs a function redeploy.
- 2026-09-24: Bug hunt 3: Pause > Restart on a Daily reloads today's Daily (`restartConfig` in rules.ts, used by main.ts restartRace), not the stored config: a session that ran past midnight UTC kept restarting the old day's seed and track, and every run posted after the 15-minute grace was refused as "that daily challenge is closed". Other modes restart exactly as they were.
- 2026-09-24: Bug hunt 3: the name filter reads words, not one joined string: roots that sit inside ordinary names (shit, dick, cock, fag, slut, rape, nazi, twat, wank) count only as a whole word or its plural (split at spaces, _, - and camelCase, single letters joined back, each word read as leetspeak and with its digits dropped), while the rarely innocent ones (fuck, cunt, nigg, bitch, whore, retard, hitler and a few compounds) still match anywhere. Dickson, Hancock, Nazim, Draper, Josh17, Smash It and Swanky pass; "S H I T", "sh1t", "BigDick" and "Nazi88" do not. Known by design: "Dick" alone and "Scunthorpe" are still refused. Shared with the server: rebuild and redeploy the function.
- 2026-09-24: Live end-to-end test (Adam's OK): one real Harbor Loop run posted as TEST was accepted (201) with the replay's time (2:02.85) and shown on the board, then deleted. It found the reply's rank empty: get_leaderboard is granted to anon and authenticated, not to the service role, so the function now reads the board with the anon key (SUPABASE_ANON_KEY).
- 2026-09-24: Detail review: a refused post shows plain words by status (`postError` in client.ts: 400 name/version/closed day, 409, 422, 429, 503, else "That did not work. Try again."), and the server's developer reason goes to the console only: the board status printed "claimed 83421 ms but the replay finished in 83433 ms". The server is unchanged.
- 2026-09-24: Red-team 2 (whole client and board): (1) one drive could be posted under many names by flipping bits the sim never reads (look-back, item, countdown steer, a throttle byte over 127), each a new canonical log. Now `canonicalize` (verify.ts) stores only what the sim read: quantised ticks, no horn, "throttle held" only in the countdown, no item or look-back in Time Trial; if the cleaned log ever replays to another time the raw log is kept. And a client (ip_hash) holds at most 3 names per board (`NAMES_PER_BOARD`, checked before the replay; 400 "you already post under 3 names on this board"). (2) Name filter: digits 2/6/8/9 fold to z/g/b/g, look-alike letters (ph→f, l→i, v→u, q→g) are tried too, `1488` and more slurs added (Kike as a Spanish nickname is refused, by design). (3) A chunked body with no length is read only up to the cap (readCapped). (4) Client: the save's records load entry by entry (store.ts sanitiseRecords). Not done, needs Adam: a `hidden` column so a row can be hidden in one line of SQL, and a live test of whether a client-set x-real-ip header gets past the rate limit.
- 2026-09-24 (Adam's OK): migration hide_scores applied live: `scores.hidden` (default false) and get_leaderboard skips hidden rows. To take a row off the board: `update public.scores set hidden = true where name = 'X';` (SQL editor or the Supabase connector). Checked: anon can call get_leaderboard, cannot select or update scores.
- 2026-09-24 (Adam's OK): live spoof test. A client-set `cf-connecting-ip` is refused by the gateway (403, the function never runs); client-set `x-real-ip` and `x-forwarded-for` are ignored because the gateway's own cf-connecting-ip comes first: 12 posts under 12 fake addresses got 400 ten times, then 429. The rate limit cannot be dodged with headers.
- 2026-09-24 (audit): the client's 12 s timeout now covers the body as well as the headers (`call` reads and parses inside it); a stalled body reads as offline, not Loading for ever.
- 2026-09-24: The replay is now bit-identical to the player's browser on every chip and engine: the sim uses `src/sim-math/dmath.ts` instead of Math transcendentals (kart-controller SOP, Lessons), proven by a per-tick dump identical on arm64 Mac, linux/arm64 and linux/amd64. CLIENT_VERSION 3 (old tabs, which ran the Math sim, are told to reload); core rebuilt with `npm run build:function`, not deployed. CLAIM_TOLERANCE_MS stays as a margin, though an honest claim should now match exactly.
- 2026-09-24: Red-team 3 (whole client and board). Checked clean, live: anon reaches only get_leaderboard (tables 401, get_ghost and take_submit_slot 401, the OpenAPI root needs the service key, pg_graphql is off, no storage buckets, RLS on both tables); verify_jwt on; the deployed core is the jsDelivr copy pinned to a full commit hash and its bytes match public/fn (SHA-256 a3df065b…); no key but the anon JWT in dist, no source maps, nothing secret in git history, `npm audit` 0; the replay uses only exact Math (clamped input, no transcendentals) and is engine-proof: one real Harbor Loop run replays to 125100 ms with the same laps and the same canonical log in V8 (node), JavaScriptCore (Safari's engine, the macOS jsc shell) and the live function (a 5 s-fast claim got 422 naming 125100 ms); a 10-minute log that never finishes replays in 0.15 to 0.5 s; the project host has no IPv6 address, so /64 rotation is moot today; save loading survives hostile JSON with no prototype pollution; names and server text are set as text. Fixed and committed: the page's Content-Security-Policy and no-referrer (index.html; ui-hud csp.test.ts). Written, NOT live (needs Adam's OK): (1) submit-score answers only the game's pages (Origin allowlist: the Pages origin and localhost; any other page 403 before it spends a slot; no Origin, as from curl, goes on to the limits): with '*' a busy page elsewhere could make every visitor's browser post, one IP apiece; (2) the body must arrive within 15 s (408), a second line: live, a stalled body was dropped by the gateway at 15 s and a trickled one (a byte every 10 s) held there past a minute, and neither started the function (no rate-limit row), so today the gateway, not the function, carries a slow upload; (3) migration 20260925000001_names_cap: a trigger recounts a client's names on a board inside the insert under a lock, and an index on (ip_hash, board); (4) the name filter (rules.ts, shared) read "Fuuuck", "Shiiit", "Fuk", "Asshole", "Bastard", "Sexy" as fine: stretched letters now squeeze (a root only meets a squeezed name if squeezing leaves the root whole, so Nigel, Bob, Essex, Cummings, Janus and Bass pass) and the everyday words are in; the game refuses them now, the server once redeployed (core rebuilt: core-019f61ef2f9600d9, same sim). Deploy: apply the migration; push; then redeploy the function with `node scripts/fn-deploy-entry.mjs` (it now refuses to print the entry if jsDelivr's bytes differ from the built core). Add the Vercel origin to ORIGINS in index.ts when the game moves there.
- 2026-09-25: The course creatures came off every track (track-builder Decisions): a sim change in every mode, the board's included (a run that met a creature replays differently). Core rebuilt (core-b0a6b4ae03123fcd) and CLIENT_VERSION 5, so a tab still running the creature build is told to reload. Not deployed: the live function needs this core when the build ships (`node scripts/fn-deploy-entry.mjs` after the push); until then it refuses the new client's posts as a new version.

## Lessons (repair loop writes here)
_(error → cause → fix → rule; newest first)_
- 2026-09-24: **Posts sent together could put 10 names on a board, not 3 (red-team 3).** Cause: submit-score counted the client's names before its replay and inserted after it, so posts that overlapped all saw the old count (the per-minute limit lets 10 through at once). Fix: trigger scores_names_cap counts again inside the insert under an advisory lock per client; the function maps its 'names per board' refusal to the same 400 (submit.test.ts holds six overlapping posts: six got on before, three now). Rule: a limit checked in one step and enforced in a later one is not a limit; the database enforces it in the write itself.
- 2026-09-23: **One valid run could fill the whole board, and a script could slip past the rate limit (red-team H1, M1).** Cause: rows were deduped only by name, the raw log was stored (post-finish padding made every copy unique), get_ghost served any run's log, and the limiter was count-then-insert over REST (racy, fail-open, per single IPv6 address). Fix: canonical logs plus a unique index, get_ghost revoked, an atomic fail-closed SQL limiter keyed by the /64. Rule: anything that limits abuse runs as one atomic database step and fails closed; store only what the server itself derived, never the client's raw bytes.
- 2026-09-23: **The input-log round-trip failed on −0.** Cause: `Math.round(−0.3 × 127) / 127` is −0 and the byte log cannot carry the sign of zero. Fix: `+ 0` in quantise and decode. Rule: anything that must replay bit for bit is normalised before the sim sees it, not after.
