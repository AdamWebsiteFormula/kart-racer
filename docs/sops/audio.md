# SOP — Audio

## Purpose
Web Audio bus graph, engine loop crossfade by RPM, drift/boost/item/UI SFX, music with final-lap lift and hit low-pass, positional opponents.

## Inputs
Race events, kart RPM, settings volumes.

## Outputs
Sound.

## Constraints
One AudioContext unlocked on first gesture; master→music/sfx→compressor; music only with commercial rights; credits in CREDITS.md.

## Tests (must pass before merge)
Manual: Safari and Chrome unlock; tab-hidden suspends; final-lap lift audible; sliders persist.

## References
web.dev Racer sound; research plan §7.3–7.4.

## Approach
_Synthesised 23 Sept 2026 from research plan §7.3–7.4, design §5, §11, the web.dev Racer sound case study as summarised in the plan, Starter-Kit-Racing (synthesised engine audio), the ui-hud settings (`masterVolume`, `musicVolume`, `sfxVolume`) and the race-manager and items event unions._

### The decision that shapes everything
**Every sound and every note is synthesised in code with Web Audio.** No sample files, no music files. The plan's music route (buy a month of Suno Pro, or self-host Stable Audio, or fall back to CC-BY) needs money, a GPU or a credit line, and each carries licence risk for a prize entry. Code we write is original and ours: zero licence risk, zero download weight, nothing to put in CREDITS.md. The cost is that the music is chiptune-flavoured rather than recorded brass; the design's ska/funk styles are written as patterns (off-beat skank chords, walking bass, brass-like filtered saw stabs, noise drums).

### The model
`src/audio/` is two layers again.
1. **Pure** (`director.ts`, `music/patterns.ts`, `music/theory.ts`, `engine.ts` maths): events and state → *cues* (plain data: which sound, which racer, what gain and pitch). Unit-tested headless.
2. **Web Audio** (`bus.ts`, `sfx.ts`, `music/player.ts`, `engine.ts` voices): one `AudioContext`, created and resumed on the first user gesture; graph `sources → sfx | music → master → DynamicsCompressor → destination`; the music bus runs through a `BiquadFilter` low-pass that dips to 700 Hz for 0.45 s when the player is hit. Suspended on `visibilitychange` hidden, resumed when visible. Volumes come from the save settings through `setVolumes()`.

**Engine.** The player's engine is two detuned oscillators (saw + square) through a low-pass whose cutoff follows RPM, plus a filtered-noise layer for tyre scrub. RPM is derived from speed over the kart's top speed with a fake gearbox (4 gears, rev drop on upshift) so it sounds like a kart, not a siren. Boost adds a bright layer. Drift screech gain follows lateral slip. The three nearest AI karts get a cheap single-oscillator engine each, stereo-panned and distance-attenuated; the rest are silent.

**SFX** (`sfx.ts`): every effect is a small patch — oscillator type, pitch envelope, gain envelope, optional noise and filter sweep — played through one generic voice. ±3 % seeded pitch variation on repeats (plan §7.4). Cues: countdown (3 low beeps, go high), lap chime, final-lap sting, finish fanfare, balloon pop, coin, roulette tick, item ready, item use per role, hit and spin, shield up/pop, air horn, fog whoosh, boost per source (drift tiers 1/2/3 rise in pitch), drift spark tier-up, hop, landing, wall bump, kart bump, wrong way, position gained/lost, UI move/confirm/back, and a horn per racer (design §5: chirp, purr-rev, chime, whistle, squeak, tick-tock, rumble, foghorn).

**Music** (`music/`): a lookahead scheduler (25 ms timer, 0.12 s horizon, `AudioContext` time) plays patterns of bars. Songs: `title` (sunny, mid-tempo), `race-sunrise` (ska: off-beat chords, walking bass, 150 bpm), `race-summit` (funk), `results` (short loop). **Final-lap lift**: on `phase: finalLap` the race song jumps up a whole tone, gains a lead layer and runs 6 % faster, on the next bar line. Countdown mutes the drums; `go` brings them in.

**Director** (`director.ts`, pure): `(RaceEvent[], ItemEvent[], playerId, UI transitions) → Cue[]`. Only the player's own item, hit, lap, position and wrong-way events are heard at full level; other racers' item uses and hits are heard only when near (distance from the camera kart), at reduced gain.

### Module boundaries (`src/audio/`)
| File | Owns | Test |
|---|---|---|
| `types.ts` | `Cue`, `SfxId`, `SongId` | — |
| `constants.ts` | Gains, envelopes, ranges, scheduler timing | sane |
| `director.ts` | Events → cues, near/far rule | each event maps; far events quiet |
| `engine.ts` | RPM from speed with gears; voice | gear shifts drop RPM; range |
| `music/theory.ts` | Note → frequency, scales, chords | A4 = 440; chord tones |
| `music/patterns.ts` | The songs as data | every bar sums to the time signature; in key |
| `music/player.ts` | Lookahead scheduler, final-lap lift | schedule math with a fake clock |
| `sfx.ts` | Patch table and the generic voice | every `SfxId` has a patch |
| `voices.ts` | One cue per sound a tick, voice caps, roulette timing | merge keeps the loudest; caps; ticks slow down |
| `bus.ts` | Context, unlock, graph, volumes, suspend | fake-context: graph wiring, volume mapping |
| `audio.ts` | `GameAudio`: the facade `main.ts` calls | — |
| `index.ts` | exports | — |

### Tests
1. Director: countdown, go, lap, final lap, finish, balloon, coin, item used, hit, shield, horn, wrong-way, position change each produce the right cue; another racer's hit 100 m away produces nothing, 10 m away a quiet cue.
2. Engine: RPM stays in [idle, redline]; accelerating through gears gives the saw-tooth RPM curve; reverse is idle-ish.
3. Theory: `freq('A4') === 440`, `freq('A5') === 880`; a C major chord is C E G.
4. Patterns: every bar of every song sums to its time signature; every note of every melodic part is in the song's key or marked chromatic.
5. Scheduler: with a fake clock, notes land at `start + beat × 60/bpm`; the final-lap lift starts on the next bar and raises every pitch a whole tone.
6. Bus: with a fake `AudioContext`, the graph is master → compressor → destination, music and sfx feed master, volumes map `master × music` onto the gains, a hidden tab suspends.
7. SFX: every `SfxId` in the union has a patch; every cue the director can emit has a patch.
8. Manual (SOP gate): Safari and Chrome unlock on the first key or click; tab-hidden suspends; the final-lap lift is audible; the sliders persist (they live in the save, ui-hud tested).

## Decisions
_(append dated one-liners as they are made)_
- 2026-09-23: Built. `src/audio/` = `types`, `constants`, `director`, `engine`, `sfx` (46 patches), `bus`, `audio` (GameAudio), `music/{theory,patterns,sequencer,instruments}`. 19 tests. Four original songs: Sunrise ska (F major, 150 bpm, 16 bars), Summit funk (E dorian, 112 bpm, 16 bars), title (G major, 118 bpm), results (C major, 100 bpm).
- 2026-09-23: No sample or music files at all; every sound is code. Nothing to credit, nothing to license, zero download weight.
- 2026-09-23: The ear sits on the camera. Pan uses the screen's right (forward × up), because with a camera looking along +z world +x is on the left of the screen.
- 2026-09-23: Menus play `title`; a race plays its cup song with the drums held until `go`; the results screen plays `results`. Other racers' hops and drifts are silent; their walls, bumps, hits, balloons and item uses are heard when within 45 m.
- 2026-09-23: Checked by rendering offline in the browser: every song peaks 0.43–0.52 with RMS 0.10–0.14 after the bus, every SFX peaks 0.06–0.67. Nothing clips; nothing is silent. The SOP's manual gate (hear it in Safari and Chrome) is still Adam's.
- 2026-09-23: Critique (Codex `gpt-5.5`), all four accepted: the scheduler starts on the context's `statechange` to running, so the first gesture is enough; the final-lap lift lands on the next bar after the event even when the lookahead already booked into it; other racers peak at `otherGain` 0.6 of the player's level; no destructuring swaps in the per-frame engine sort.

- 2026-09-23: Recorded audio from ElevenLabs (scripts/elevenlabs: catalog.ts holds every prompt, generate.ts makes the files and public/audio/manifest.json). 58 sound effects cost 643 credits; 7 songs (9.6 min) with music_v2_5. The synth stays as the fallback for anything missing or not decoded yet.
- 2026-09-23: Every recording is levelled in the browser at decode time (loudest 50 ms for effects, mean for loops and songs), then set by one mix table (samples.ts MIX), so no audio tool is needed offline.
- 2026-09-23: Songs loop on whole bars: the bar is measured from the onsets near the tempo the prompt asked for, the loop ends on the last 4-bar phrase before the fade, and is nudged ±80 ms to line the beats up. The pass before rings on 150 ms past the loop point.
- 2026-09-23: Race songs start on the go, not during the countdown. Final lap: the song stops for the fanfare, then comes back from the top at ×1.06 (a semitone up, the classic lift).
- 2026-09-23: The engine is three recorded loops (idle, mid, high) crossfaded at equal power by rpm, pitched by rpm / band rpm, plus a recorded drift screech (plan §7.4). Rivals use the mid loop, panned.
- 2026-09-23: Every racer has a hit yelp (design §11): creature noises, never words. Only cast racers yelp.
- 2026-09-24: The final-lap fanfare and the music lift play on the player's own last lap (their `lap` event with `isFinal`), not on the shift (the leader's): a trailing player used to hear the fanfare up to 4.8 s early and nothing on their real final lap. The two stay together because `SongPlayer.lift` pauses the song for the fanfare.
- 2026-09-24: Race cues stop when the race screen closes (main.ts calls `audio.tick` only while `racing`): the sim keeps the field driving under the results, GP table and Knockout cut, and its bumps, pads and creatures played on over the results song for as long as it stayed open, up to 8 bumps a second (the engines were already quiet there).
- 2026-09-24: Gained or lost place is measured from the player's grid rank (`newRace(song, trackId, gridRank)` seeds the director from the tracker's `shownRank` at load): with no seed the first pass off the back row played `losePlace` every race.
- 2026-09-24: One `bump` cue per kart contact: collide.ts raises one on each kart, so the player's partner and the second of two other karts (by racer id) stay quiet. The player's rubs no longer thud twice, and the jostle after the go makes half the bonks (it reached 16-22 a second).
- 2026-09-24: Hazard hits with no kart event are heard through `hazardHit`: a bumper car's shove plays `bump`, a rockfall's slow plays `hit` (a spin already plays through its kart `hit`, a vent's launch through its own cue). Both used to be silent.
- 2026-09-24: Seam review: the finish fanfare follows the race's own winning line (`finishLine(config)`, passed by main.ts through `newRace(…, finishLine)`): a Knockout round's cut line, only 1st in its final (the HUD's WIN THE FINAL), else the podium (`AUDIO.podium`, 3). It was top 3 everywhere, so 2nd in the Knockout final heard the victory fanfare and was then shown OUT, and a safe 4th to 6th heard the nice-try jingle before "Safe!". A solo run's one racer is 1st, so Time Trial and Daily keep the fanfare.

- 2026-09-24: `?mute` on the address gives the game a silent bus (AudioBus.silent: no audio context is ever built), so automated browser checks make no sound. Every agent loads the game with it and closes its tabs when done (CLAUDE.md, Browser checks). Why: a hidden test page played the title music at night in Adam's home.
- 2026-09-24: Detail review (no singing, no human voices, a Mario Kart World feel): the `loop` sound no longer asks for a crowd shouting (remade); tests hold every song prompt to "Instrumental, no vocals", every song request to `force_instrumental: true` (catalog `songBody`, the only place generate.ts gets it) and every sound prompt to no crowd, cheer, chant, shout or singing. The seven songs were not remade: they already carry both, and swapping an approved soundtrack unheard was not worth it (Adam's headphone check is the last word). Crossing the line fades the race song (`finishFade`) so the sting plays alone, and the results song waits for its last chord (`STING_SECONDS`). The pause menu drops the music to 30 % behind a 1.2 kHz low-pass (`AUDIO.pause`). New cues, recorded and synth: `hitConfirm` (your item lands on a rival, full level however far, once a tick), `slipstream`, `tierUp2`/`tierUp3` (each spark tier its own zap, pitched ×1 / 1.12 / 1.26) and the `offroad` rumble loop on dirt and mud (synth noise stands in when the loop is missing).
- 2026-09-24: Audit fixes: new cues `shift` (Final Lap Shift, full level, music dips), `koSafe`/`koOut` (`GameAudio.knockout(out)` from main.ts `raceOver`, results song waits), `trick` (new kart event when the trick is done), `bounce`, `pop`, `shieldEnd`; `rocket` removed (design has no rockets); one cue per sound a tick, 3 voices a sound, 24 in all, a -3 dB 20:1 limiter after the compressor, level gain held so no sample peak passes 0.9; songs and loops wrap sample-accurately with a baked equal-power crossfade (12 ms songs, 30 ms loops) instead of a 150 ms overlapping second pass; rivals each start the loop at their own point and sit ±2 % apart in pitch; the recorded engine runs through an rpm-tracking low-pass (2.5 kHz at idle); synth engines are built only in a race and stopped and unplugged when the recordings take over; title and results stay decoded plus two race songs; no pitch jitter on musical and menu cues; big sounds dip the music 6 dB (50 ms down, 400 ms back); distance gain falls smoothly (no 9 dB step at 12 m); roulette ticks slow from 60 to 220 ms, each cutting the last; `uiBack` remade (it was -27.6 dBFS).
- 2026-09-24: The mix is judged offline, never by ear on this machine (nobody may play a sound here). `scripts/elevenlabs/mix/` drives the real GameAudio, director, bus and samples through a whole headless 8-kart race into a faithful offline Web Audio (a-rate gains, biquads, panner, Chromium's DynamicsCompressor kernel with its makeup gain; MP3s decoded by macOS `afconvert`) and writes the mix and its stems (music, engines, cues) as WAV; `measure` reads BS.1770 loudness, 4× true peak, LRA, balance per race phase, octave balance, stereo, and each cue's loudest 100 ms against the music + engine bed at that moment (whole band and 1–4 kHz); `assets` measures every recording as the game plays it. From the repo root: `npx vitest run --config scripts/elevenlabs/mix/vitest.config.mts render` (then `measure`, `assets`); TRACK, PLAYER, SEED, TAG, FULL=1 (every slider at full), MIX_OUT.
- 2026-09-24: Before → after, the same six races (HEAD sim, seed 11, Pip, default sliders): integrated −15.0…−16.2 → −15.0…−16.7 LUFS; true peak worst −1.0 → −1.3 dBTP (every slider at full: 0.0 → −0.7 dBTP, no clipped sample either way); your gameplay cues over the bed, median −2.9 → +3.2 dB (p10 −15.1 → +0.1), in 1–4 kHz −10.4 → +4.0 dB; cues under the bed 1,192 of 1,627 → 184 of 1,873; buried more than 12 dB under it 294 → 0; every one of your cues has its median over the bed; rivals' cues −9.3 → −2.9 dB, still 6 dB under yours; LRA 2.7–6.7 → 4.7–8.1 LU; music over engines 3.0–4.6 → 2.0–3.9 dB; roulette ticks played 56 → 98 of 98. The local ear (CLAP, no playback) names your cue over "racing music" and "engine" at 75 % of its moments (was 44 %).
- 2026-09-24: Every recording is levelled on its loudness as heard (K-weighted, `kWeight`: a sound effect's loudest 100 ms, a loop's average, a song's loop read from four 4 s stretches, `songLevel`), not raw RMS: a bassy stomp and a bright zap at the same RMS differ by 5 dB to the ear. The mix table is now dB (`MIX_DB`, −8 to +1). One-shots within ±2 LU of the common level with the table taken out: 47 → 71 of 91 (the rest are sharp transients held down by the peak ceiling); the seven songs now within 0.7 LU (were 2.3 apart).
- 2026-09-24: Every one-shot is shaped at load (`cutSfx`): DC taken out (12 had over −50 dB), a 2 ms fade-in at the cut (51 began mid-wave), the end moved to −60 dB and faded (16 ended on a non-zero sample; the 7 cut off loud get 90 ms). The start is cut on the level of 2 ms windows, 20 dB under the peak for the thuds and zaps (`TIGHT`): the bump's first 110 ms were scrape noise, so every bump landed late (124 → 5 ms to its peak), the throw 85 → 32 ms, the trail 57 → 5 ms. Swells (the shift, the kite, the whale) keep their build. No one-shot has hiss in its quiet parts (quietest 50 ms inside each sound: none both over −60 dBFS and noise-like).
- 2026-09-24: A loop that swells and dips by more than 2.5 dB (100 ms level, standard deviation) is evened before its wrap is baked (`evenLoop`: drift, off-road, snow), so it does not pulse every three seconds; the engines' lope is left alone.
- 2026-09-24: Bus: a −3 dB presence dip on the music (2.5 kHz, Q 0.8) so the cues are not masked where they live; a 16 kHz low-pass on the whole mix before the dynamics (sparkles' inter-sample peaks passed the limiter by 1.3 dB); the limiter at −6 dB (Chrome's compressor adds makeup: +4.2 dB on the compressor, +3.4 dB on the limiter); master 1.1 at full slider. At the same output loudness the music now sits 4–6 dB and the engines 3–4 dB lower under the cues (stems at the dynamics, all six races); each song is about −19.3 LUFS before the bus.
- 2026-09-24: The roulette stuttered: a cut tick still held one of its sound's three voices, so 42 of 98 ticks in a race were refused. A cut tick now frees its voice (`Voices.release`).
- 2026-09-24: Mario Kart World touches: engines by class (`classVoice`: light ×1.12 and brighter, heavy ×0.84 and darker; the same Harbour race's engine centroid light 1,660 Hz, medium 1,530, heavy 1,330); a boost revs the engine (`boostRev`: the loops jump 12 % and swell, settling over 0.35 s, on top of the boost's own rpm; tested on the loops' rates, a modest lift in the whole-race engine stem because nearby rivals and the wheels share it); the drift sparks crackle from the first tier, louder and a tone higher each tier (`sparkLayer`, the `sparks` loop: 13–28 s a race); the wheels sound like the ground (`wheelSound`): sand beside Harbour and Canyon, grass and dirt at Meadow, snow at Frostbite, ice on Frostbite's lake (35 s a race), the planks of the whole Boardwalk (133 s), the grind on Skyline's rail (15 s). Each surface loop starts the first time the wheels touch it.
- 2026-09-24: ElevenLabs, 335 credits: new loops `offroad-sand`, `offroad-snow` (twice: the first pulsed), `road-ice`, `road-wood`, `rail-grind`, `sparks`; remade `rouletteTick` (a 5 ms click so spiky that, set where you could hear it, it pushed the mix past −1 dBTP; now a pitched wooden tock), `boostStart` (1.6 s: the 1.2 s take stopped at full level), `bounce` (four takes: two near-silent, the kept one the least metallic). Two more takes each of `boost1`–`3`, `drop` and `pop` were scored by the local ear: only one `drop` beat the old take (it had tagged as a coin); the rest were kept.
- 2026-09-24: Local listening pass (Qwen2.5-Omni, AST, CLAP; nothing played): no song has a voice, word or singing in any 10 s window, and no song's loop seam has a click, glitch or break in the rhythm; no yelp or blip scores as speech. The models hear short whooshes poorly (boosts, the trick, the slipstream score as coins or clicks), so those are on Adam's listening list, with the music-to-engine balance (engines now 2–4 dB under the music) and the 8 kHz octave (about 5 dB brighter than a pop reference, the same as before).
- 2026-09-24: Intent pass (the local ear, `scripts/ear`, nothing played): CLAP ranked each sound's own catalog prompt among all 102, and 18 sounds ranked low. Each suspect got two opinions, CLAP among five plain texts (the intended one and its confusions) and Qwen2.5-Omni's free description; the 10 both heard as something else were remade, and a take was kept only when it ranked near the top on the intent check, Qwen described it right, CLAP's five-way test agreed (all but `trail`, which CLAP still calls a zap, as it does any quick click) and nothing about it read as a weapon (G-rated): `slipstream` (a gun being reloaded, 0.61 → a whoosh, "a gust of wind"), `boost1`/`boost2`/`boost3` (a bell, a beep, a train horn → whooshes, each tier bigger), `kite` (a slide whistle → papery flutter), `anchor` (a door slam or an explosion → a chain jangling and a clank), `coin` (a click, a hammer on metal → a bright ding), `trail` (a dropped coin → a plastic click), `horn:sprocket` (a bell only → a wind-up ratchet and a ding), `shieldEnd` (a boing and a pop → a fading shimmer). Own-prompt rank before → after: slipstream 46 → 5, kite 73 → 2, boost1 70 → 6, boost2 30 → 3, boost3 29 → 12, anchor 69 → 1, coin 62 → 9, trail 43 → 1, horn:sprocket 42 → 2, shieldEnd 41 → 3; all 102 sounds: 1st 30 → 32, top five 60 → 66. 3 to 12 takes each (the kite went through four prompts, the slipstream three: naming the speeding kart brought an engine in, the flag-in-a-gale kite read as door slams; the slipstream kept is the first new take), 516 credits.
- 2026-09-24: Kept where the two ears disagreed (CLAP heard the intent, Qwen did not, or the other way): `losePlace` (Qwen: a bird), `tierUp` (Qwen: a dropped coin), `slingshot` and `trick` (Qwen: a pop), `boostPad` (a burst of air), `gainPlace` (a siren), `uiBack` (a click), `yelp:pip` (CLAP: a zap, Qwen: a squeak). They are on Adam's listening list.
- 2026-09-24: The remade boosts and the slipstream swell for 100–250 ms before their peak; the kart jumps the instant a boost fires, so their start is cut 12 dB under the peak (`PUNCH`, `cutDb`): time to within 12 dB of the peak 34–118 ms → 3–4 ms. `shieldEnd` back to −3 dB (the new take is not buried). Three races before → after the remakes: your cues over the bed, median +3.7 → +3.8 dB, none buried; boost1 +0.7 → +4.3 dB, slipstream +3.6 → +4.8; integrated −15.8 to −16.8 LUFS, true peak −1.8 dBTP or lower.
- 2026-09-24: The judge (`scripts/ear/judge.mjs`, Gemini Pro, nothing played): each sound goes up with its catalog prompt and its in-game moment (`MOMENT` in the catalog, one per sound, a test holds it) and comes back as strict JSON (response schema): what it heard, 0–10 against the brief, 0–10 for its moment at Mario Kart World quality, problems, keep or remake. It batches clips (`--batch=6`, `songset`), compares takes (`compare`), checks a model on known answers first (`calibrate`), logs every call's cost, and pads every clip with silence to 2.5 s: a 0.5 s clip was heard as "no audio was provided".
- 2026-09-24: The Final Lap Shift remade: the old take was a whoosh under a second long (the judge: 1/10 against the brief, 1/10 for the moment, "a UI transition"). 14 takes over four prompts; the judge compared them and picked one at 10/10 and 9/10 ("a deep rumble that rises into a sweeping whoosh, then bright magical chimes that ring out"), 4.5 s, and the local ears agree. The music now stays down under it (`AUDIO.shiftDuck`: about −9 dB, held for 60 % of its length, then back under the shimmer): when the player trails the leader, the music sits 7–18 dB lower through the shift's body in the render; when the player leads, the final-lap pause frames it anyway. Mix unchanged (−15.7 to −16.8 LUFS, true peak −1.8 dBTP or lower).
- 2026-09-24: The judge's first pass reached 19 of 102 sounds and 9 of 21 song excerpts before the key's daily Pro cap (250 requests). Scores 9–10: count, lap, finalLap, finish, finishLow, koOut, rouletteTick, shieldUp, pop, throw, airHorn, coin, kite. Remake: `go` (2/2, a dry trumpet; the local ears hear a car horn), `balloon` (3/2, a dry firecracker; locals agree), `shieldPop` (3/4, a wet splat; Qwen agrees), `itemReady` (2/3, one ping, not an arpeggio; Qwen agrees, CLAP not), `koSafe` (3/4, too slight; the local ears disagree). Three new takes each of the first four wait in ~/.cache/rascal-ear/takes/2026-09-24 (prompts in pending.json) for the judge to pick tomorrow; the shipped files and prompts are unchanged until then. gemini-3.8-flash failed the known-answer check (the crab's claws as "a squeak and a tap"), so it judged nothing. Pending for tomorrow: 83 sounds (pending-judge.txt), 12 song excerpts, those four picks, koSafe.
- 2026-09-24: Songs (report only, no remakes without Adam): title, Harbour and Meadow judged 8–10 for mood and 6–9 for energy, seams clean. The judge heard "vocal shouts" at the start of the title song and "cheering voices" mid-way through the Harbour theme; AST (2 s windows: no shout, cheer or chant label over 3 %) and Qwen ("no human voice") do not, so it is on Adam's listening list with the judge asked for timestamps tomorrow. Meadow's opening lacks the brass lead its brief asks for (energy 6/10).
- 2026-09-24: Spend today: Gemini $0.31 by this ledger (ElevenLabs 675 credits for the shift and the pending takes).
- 2026-09-24 (podium ceremony): `GameAudio.ceremony(onPodium)`: the results song fades for the victory sting (`finish`) when the player stands on the podium, the friendly one (`finishLow`) when not, and comes back after its last chord. No new recordings; silent under ?mute like everything else.
- 2026-09-25: Sound files come down in turn (samples.ts SFX_TIERS: the menus' clicks; a race's first seconds: count, go, the engine loops, drift, sparks, hop and land, the tier-ups, the boosts; the items and hits with the yelps; everything else), each through `SampleBank.schedule` into the game's one background line (performance/loadQueue.ts, main.ts RANK: a sound effect half a turn, a song a whole one, measured in docs/sops/performance.md). Songs only when asked: the title on the first key press, a race's as it loads, the results at the finish. The recordings' list is enough to ask for a song (`onManifest`); until its recording is decoded the synth plays it (`GameAudio.play`, and on the go), then the recording takes over: the title went silent from the list's arrival until its file came down, and a race song not in by the go was silence. A sound effect not in yet plays its synth patch, as before. Fast 4G: the title song 2.5-2.8 s after the first key press (was 7.2-7.4 s). `kart.audioLoad()` (dev) runs the real bank through the real line on a stand-in context that decodes to silence and plays nothing, for ?mute checks; `kart.audioSong(trackId)` asks for a race's song the same way.

## Lessons (repair loop writes here)
_(error → cause → fix → rule; newest first)_
- 2026-09-24: **The local ears missed the Final Lap Shift.** Cause: CLAP scores whether a sound matches words, not whether it is big enough for its moment; the 0.8 s whoosh matched "rumble, whoosh, shimmer" at 0.99. Fix: a judge that hears the sound against its moment (Gemini Pro, `MOMENT`). Rule: judge a sound against where it plays, not only against what it was asked to be.
- 2026-09-24: **A new take can match its prompt and still be wrong for the game.** Cause: ElevenLabs followed the words it was given: "turbo" and "jet" rang like bells and horns, a "speeding go-kart" brought an engine into a wind whoosh, a "flag in a gale" snapped like a door. Fix: describe the sound, not the object making it, and judge takes with more than one ear (CLAP five-way, the intent rank, Qwen's description, a weapon check). Rule: no take replaces a sound on one classifier's word; CLAP alone hears any quick whoosh as "a bullet whizzing past" when that is on its list.
- 2026-09-24: **The mix measured loud enough, but your own cues were buried.** Cause: every recording was levelled on raw RMS and set by a guessed table; in a whole race three quarters of your cues sat under the music and engines, 294 more than 12 dB under. Fix: render whole races offline, measure each cue against the bed at its moment, level on K-weighted loudness, dip the music's presence band. Rule: judge a mix by rendering it and measuring cue against bed, never by the level of each file alone.
- 2026-09-24: **Chrome's DynamicsCompressorNode is not unity gain.** It adds makeup gain, (1 / its own output at 0 dBFS)^0.6: +4.2 dB for our compressor, +3.4 dB for the −6 dB limiter. Rule: count it in any level maths and in the limiter's ceiling; the offline renderer models it.
- 2026-09-24: **A generated take can come back near-silent** (two of four `bounce` takes peaked at −34 and −26 dBFS). Rule: measure every new take (peak, loudness) before scoring how it sounds, and never keep one on a classifier's word alone.
- 2026-09-23: **Recorded songs came out a third as loud as the synth songs (−9 dB).** Cause: a mastered recording has far more peak for its average than a synth loop, so levelling it to a quiet mean left it thin. Fix: measure both through an AnalyserNode on the music bus with the master at zero, and raise the song target. Rule: level new audio against what it replaces by measurement, with the master muted, never by guesswork.
- 2026-09-23: **Pan was mirrored.** Cause: used the kart's `rightOf` (up × forward = +x at heading 0); the camera's screen right is forward × up, the opposite. Fix: `right = −dx cos h + dz sin h`. Rule: for anything the player hears or sees, derive left/right from the camera, not the kart.
- 2026-09-23: **A sequencer test failed one run in some.** Cause: the window ended exactly on a note, and `beat × 60 / bpm` round-trips with a rounding error either side. Fix: window edges between notes. Rule: never put a test boundary on a grid value that went through floating-point maths.
