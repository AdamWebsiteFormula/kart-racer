# SOP — Art pipeline

## Purpose
Kitbash CC0 kits in Blender → GLB → gltf-transform optimize → toon materials, outlines, palettes per biome, sky presets, LUTs.

## Inputs
Kenney/Quaternius sources, design bible palettes.

## Outputs
assets/models/*.glb, assets/textures, sky and LUT presets, CREDITS.md entries.

## Constraints
One shared palette texture where possible; every asset licence recorded before import; no AI-generated character meshes; no Nintendo look-alikes.

## Tests (must pass before merge)
Every GLB under 2 MB; total models under 12 MB; each racer passes the 32 px silhouette test (screenshot in docs/silhouettes/).

## References
research plan §6.3, §7.1; Design Loop critique per biome.

## Approach
_Written 23 Sept 2026 alongside the build (the ritual's research step was folded in: plan §6.3, §7.1, design §3–§6, the track-builder `TrackAssets` hook and `game/kartMesh.ts`)._

**Every model is built in code** from coloured primitives (`ModelBuilder`: box, ball, cylinder, cone, torus, rock, eye), merged into **one vertex-coloured geometry** plus **one ink hull** (each part inflated by an absolute ink width in its own frame, so outlines are even and crack-free). One shared `MeshToonMaterial` with a 3-step `NearestFilter` ramp; one shared unlit back-face ink material. Result: two draw calls per kart, two per decor type, whatever the part count.
- `racers.ts`: the eight signature karts and drivers from design §4–§5, origin on the ground facing +Z, inside the kart footprint.
- `decor.ts`: models keyed exactly as `TrackAssets` expects (decor ids, `<biome>-barrier`, landmark id, `balloon`, `coin`, `boostPad`, `ramp`, hazard assets), each keeping its placeholder's size and origin so no placement code changes.
- `sky.ts`: a painted two-band gradient dome with a soft sun, presets by `environment.sky` id; the Final Lap Shift's `sky` swaps the preset. Fog takes the horizon colour so the far road melts into the sky.
- Tests: footprint, triangle budgets per instance, vertex colours, hulls, and the SOP's 32 px silhouette test (every pair of racers must differ in side or front view; worst pair IoU < 0.86). `WRITE_SILHOUETTES=1` refreshes `docs/silhouettes/`.

## Decisions
_(append dated one-liners as they are made)_
- 2026-09-23: **No imported kits.** Kenney and Quaternius needed downloads and Blender; modelling from primitives in code needs neither, has no licence to record, weighs nothing, and is not a text-to-3D mesh (the design's ban). The SOP's "every GLB under 2 MB / total under 12 MB" gates are moot: there are no GLBs.
- 2026-09-23: Draw calls 102 → 56 in a race (karts 7 → 2 each). Triangles held near 340 k including the shadow pass: the 1,188 harbour bollards are six-sided with no outline (~60 triangles each); palms ~1,000 with ink; per-model budgets are in `art.test.ts`.
- 2026-09-23: Track-builder change, the only one outside `src/art-pipeline`: `TrackAssets` gained `hulls`, `ink` and `gradientMap`; the scene uses a geometry's own vertex colours when it has them; decor, barrier and landmark hulls share their model's instance matrices; retiring a mesh never disposes the shared ink.
- 2026-09-23: Decor for Meadow Run (windmill landmark, small windmills, oaks, fences, a red barn, hay-bale kerbs, rolling hay bales) and Canyon Rush (rock arch landmark, cacti, rocks, mesas, turquoise-banded kerbs, mine carts, falling rocks), plus the `canyon-dusk` sky, modelled ahead of the track files.

- 2026-09-23: `Paint` accepts linear RGB with values above 1, so lanterns, neon and lit windows glow through the bloom with no extra material or draw call.
- 2026-09-23: Summit Cup decor modelled (Frostbite, Skyline, Boardwalk). Hazard models are built centred with their base 1.2 m down, because the scene lifts a hazard's centre by its radius.
- 2026-09-23: The sky dome follows the camera, so the horizon sits at eye level on any track height. On tracks with no ground, the hemisphere light's lower colour is the sky's lower band, not earth brown.
- 2026-09-23: Every kart has exhaust pipes (racers.ts EXHAUST: ports, direction, flame color). Side pipes splay 0.35 outward so the chase camera sees the flames as a V, not two dots. Nova's thruster and Boulder's truck stacks are their exhausts. Flame color is the racer's accent, or their second color where the accent is too dark to burn (Momo, Boulder).
- 2026-09-23: Outlines are soft and colored, not black (the user's call): each hull part is a deep shade of its own color (same hue, richer, sRGB lightness × 0.4, capped at linear luminance 0.1 so yellow is as deep as blue), drawn with one shared vertex-colored back-face material. The racers' ink is thinner (0.028 m) than the scenery's.
- 2026-09-23: No outlines at all (the user's call, twice: Mario Kart World draws none). Racers and scenery render their body mesh only; `ModelBuilder.outline()` stays as an unused option. Harbor Loop dropped from 76 to 69 draw calls.
- 2026-09-23: Painted skies: 12 AI panoramas (GPT Image 2.5 via Higgsfield, 21:9 at 2K, medium quality, 1 credit each) in public/skies, wrapped once round the dome from the horizon to 0.85 rad up, the one seam hidden by a mirrored blend, a little saturation added before tone mapping. Each preset's horizon and top colours are measured from its painting (scripts/skies/prepare.py), so the fog melts into it.
- 2026-09-24 (detail review 5): code-modelled verge ground cover in toon style, each under 250 triangles and at most about 1.1 m tall (karts drive through it): `tuft`, `flowers`, `bush`, `scrub`, `pebbles`, `sapling`, `stones`; and Harbor's course-limit clutter `crate`, `umbrella` (two four-sided canopies a quarter turn apart read as coral and white gores), `rope-post`. Budgets in art.test.ts.
- 2026-09-24: Detail review, skies (sky.ts): Canyon's final-lap dusk lights with a pale peach key and violet fill (`canyon-dusk` sun #ffc49a 1.7, sky #8f86e0 0.9, ambient #8a7ad0 0.55) and a SkyLight `grade` of 0.03 (post colour lift, DAY_GRADE 0.12 elsewhere), so the sand and road keep their detail instead of one clipped red. Night and dusk skies carry a `glow` for the pickups (boardwalk 0.5/0.4, skyline night 0.3, canyon dusk 0.12). The Skyline paintings put their own horizon about a quarter up the file (`panoHorizon` 0.26/0.24): the dome maps it onto the real horizon and paints the cloud sea under it over the first 0.35 rad below. A Final Lap Shift fades the old sky into the new one in the same material over SKY_FADE 1.5 s (fadeSky), and the shift's painting is fetched and uploaded as the race loads (preloadSky), so the shift no longer shows a flat gradient. `skyTint` gives the horizon ring's tint for a sky change (ratio of the two skies' light).

- 2026-09-24: Pickup balloon redrawn (decor.ts `balloon`): one glossy color (#ff2e63), a teardrop body, a white shine on each face, a knot and a wavy white ribbon. The old eight striped gores read as a beach ball (Adam). Never stripe a pickup balloon; the Beach Ball item owns stripes. The Decoy Balloon shares this model.
- 2026-09-24: Mario Kart World density for Frostbite Pass and Canyon Rush (they read as a flat white plain and an empty orange plain). 31 code-built toon models in `dressing.ts` (60 to 670 triangles): snow firs in stands of three, snow fences, lantern posts, pastel cottages, igloos, sleds, ice sculptures and shards, crags, a frozen waterfall, a chairlift, snowdrifts, pink chevron corner signs and bunting; saguaros (two), barrel cacti, tumbleweeds, hoodoos, banded cliff walls, mine rails with ore carts, a water tower, a windpump, adobe huts, a signpost, a ranch fence, dunes, turquoise chevron signs, bunting and a rock arch over the road. Placed in three layers: the course limit (fences, lamps, signs, villages, 12 to 19 m past the curb), the verge (drive-through cover), and far (forests, crags, hoodoos, set-pieces, ground relief out to 180 m). No imported models, no words on signs.
- 2026-09-24: Track-builder change for it: decor entries gained `layout` (`row`: runs of pieces along the road, +X away from it; `span`: across the road, legs stretched past the course limit, off-road tracks only), `at`, `side`, `dist`, `scale` and `merge`. A `merge` entry (a code-built, vertex-coloured model only) is baked into the track's merged dressing (mesh/merge.ts): 4 slices round the track that cast shadows (roadside, spans) and 2 that do not (far, verge), each one static mesh with its own toon material, so 20 new kinds cost 6 draws plus 4 shadow draws. Headless (frameBudget.test.ts counting): Frostbite 63 → 73 draws with shadows, 195 k → 339 k triangles; Canyon 62 → 72, 183 k → 301 k. The AI-model pines (130, was 180), cacti (45, was 70) and rocks (90, was 110) were thinned to pay for it in the browser.
- 2026-09-24: Lighter pass on Meadow Run (ranch fences, sunflower clumps, yellow chevrons at the hairpin, a water tower, windpumps, signposts, two bunting spans, grassy knolls; 57 → 67 draws with shadows, 249 k → 288 k triangles) and Harbor Loop (coral chevrons at turn one, three bunting spans; 66 → 70, 275 k → 279 k). None for Boardwalk Nights and Skyline Circuit: a pier and a sky road have no course limit for a span's legs, and they were already the densest per metre.
- 2026-09-24: Visual review of the density pass. Canyon cliff walls rebuilt twice (stacked cubes, then stacked drums) into long rounded sandstone strata that step out and in, with domed tops of uneven height. Frostbite's snow gets blue hollows, wind ripples and near-camera glints in world space, on the ground plane and the land alike (surfaces.ts `snowShader`, no new draws). Frostbite's far ring is three ranges (backdrop.ts `paint`, `snowline`): dark forested foothills at 580 m, grey-blue rock with a jagged snow line, rock ribs and snow gullies at 690 m, pale blue peaks at 800 m; about 35 k triangles, was 9 k. The long climb (t 0.40 to 0.56) gets a ski lodge, a ski-jump tower, crags, firs, lamps and frozen grass tufts on the driver's left.
- 2026-09-24: Placement fixes from the review: row pieces turn and stretch along the chord to their neighbours, so fences meet on bends; a small merged prop (claim under 1.2 m: a fence, a lamp, a sign) gives way to props placed before it (decor.ts `Occupancy`), so no fence runs through a cactus or cottage; a span may `keepShape` (the rock arch grows whole instead of stretching); footing `sink` lets a set-piece with a 3.5 m foundation stand on a slope. Note: the track-builder's side -1 ("left") is the driver's right (+X is screen-left looking down +Z), so a set-piece on the driver's left takes `"side": "right"`.
- 2026-09-24: Far vistas on all six tracks (Adam: "make sure the distance of the environment looks interesting ... take inspiration from Mario Kart World"), vista.ts through a new TrackAssets `vista` hook. Three depth layers: code-built set-pieces 40 to 330 m past the farthest road (one world-space toon mesh, hazed at 0.55 of the fog's rate so a landmark reads at 500 m, no shadow), then two or three backdrop rings bluer and hazier with distance. Harbor: a dormant volcano island with palms and smoke (the landmark ahead of the start), a headland lighthouse, a hillside town, a bay bridge between rocky shores, sea stacks, sailboats; rings of blue mountains, cliffed headlands, beach islets. Meadow: a giant lone tree, windmill hills with turning sails, a river with a stone bridge and glints, patchwork fields, hot-air balloons; rings of snow-capped blue mountains and patchwork hills. Canyon: a cinder cone with a glowing crater and dust, a train trestle with a train, a rope bridge between hoodoos, a gorge mouth with a river, butte clusters; rings of violet peaks, banded mesas, red buttes. Frostbite: a glacier peak, fir ridges, a giant frozen fall, a lit village and a cable car. Boardwalk: an island tower with lit windows and a beacon, a lighthouse with a sweeping beam, neon skyscrapers, fireworks, a moon with a halo; night hills. Skyline: floating islands with waterfalls and mist, a sky city with gold domes, airships.
- 2026-09-24: What moves out there is one mesh posed in its vertex shader from the water clock (orbit, drift, shuttle, turn), what glows one additive mesh (sparks, smoke, beams, bands of light, blinks), so a vista costs at most three draws (Harbor 68 → 73 with shadows, Frostbite 73 → 75, all six ≤ 75); the performance warm-up compiles them with the scene (they are in it from the start). Each track's far landmark sits within 35° of the start line's heading and under 700 m (vista.test.ts, Mirror mode too). Frostbite's far firs use a two-tier `fir-far` stand to pay for it (352 k triangles, was 375 k).
- 2026-09-24: Sky life (Adam's approval; researched against Mario Kart World: one kind of ambient life a course, sometimes two, far, slow and small). Harbor: five gulls wheeling 28 to 44 m over the water, and three on posts at the sea's edge along the start straight that fly off out to sea as the camera comes within 30 m and are back, unseen, by the next lap (the 30 frozen gulls are gone). Meadow: the five hot-air balloons are gone (they read as item balloons); five songbirds on a fence of the start straight flutter off as you pass; a V of five geese crosses the quiet north straight 70 m up, sideways, once a lap; the storm sends them all away. Canyon: three pterosaurs of our own (sandy, two-panel wings, a short swept crest) circle 76 to 96 m over the far buttes. Frostbite: a pair of dark eagles glides round the far peak, away from the Yeti, and leaves when the blizzard starts. Boardwalk: fireworks from two points over the sea, a burst every 5 s on laps one and two; the Final Lap Shift opens three more (1.0 bursts a second in all, under WCAG 2.3.1's three). Skyline: three airships at least 145 m out (the six parked ones are gone), their windows lit only once the sky turns to night.
- 2026-09-24: How: one movers draw; birds flap in the vertex shader (whatever lies past the body's half-width turns about it, folded on a perch), orbit, perch-and-go, flyby and leave paths are shader kinds read from 16 trigger slots. The movers mesh's onBeforeRender hands the camera's place to the triggers, so no game code changed; the governor's Low (no shadow map) collapses every winged flier first. Fliers cast no shadow, are fogged, and are low in saturation; vista.test.ts checks no more than eight at least 2 px across in view anywhere on the lap, 25 m over the road or 30 m past the course limit, flybys crossing a road sideways only, never larger on screen than a pickup balloon, perched birds leaving away from the road, the triggers (once a lap, back unseen, silent once the weather turns) and the fireworks' cadence.
- 2026-09-24: A roadside prop on an off-road track stands CLEAR_EPS (0.1 m) past the course limit plus its footprint: at exactly the limit the road-envelope test rejected it about half the time, so rows at the limit never filled a corner.
- 2026-09-24: Alt paints (`paints.ts`, design §10): a palette swap, not a tint. A paint is two or three rules, each taking one colour family (hue, saturation and value bands with soft edges) to a new hue, saturation and value, so shading and texture detail stay and eyes, tyres and whites are left alone. Pip Berry (teal to raspberry, coral to marigold), Boulder Frost (slate to a cold blue-grey, moss to snow), Sprocket Mint (cream to mint, brass to copper). A model file's colour texture is repainted once through a canvas into one shared material per racer and paint (`RacerModels.paintMaterial`, `userData.shared`, never disposed by a race); a code-built model's vertex colours go through the same rules (`ModelBuilder.recolor`). No extra draw call. Tuned by eye on software renders of the real textures.
- 2026-09-24: Shared bodies (`bodies.ts`, design §5): Classic (an open-wheel kart with a bullet nose, side pods and a rear wing) and Buggy (chunky tyres, fenders, a roll bar behind the seat, headlamps, a bare engine), code-modelled in the racer's two colours (or the paint's), inside the kart footprint with the wheels where the signature karts have theirs. The driver is cut out of the racer's model file (`glb.ts DRIVER_CUTS`, `clipDriver`: triangles by their centres above a hip line, inside a box, minus drop boxes for an awning or a roll cage) and seated at `SEAT`, its cut under the padded cockpit rim; without a model file the code-built driver sits there instead (one merged mesh). With a model file, a body kart is two draw calls (body and driver) where the signature kart is one; only the player's kart and the ghost wear one. Each body has its own exhaust ports (`BODY_EXHAUST`, on `userData.exhaust`), the flame in the racer's colour.
- 2026-09-24: `public/models/manifest.json` yaws for Juniper (π), Otto (0) and Sprocket (0) fixed: at −π/2 like the others, those three model files faced sideways in the kart frame (their source files face another way than Pip's), so they drove crabwise and were fitted small. Found rendering every model file in the fitted frame; all eight now face +Z.
- 2026-09-24: Boost flames burn in the alt paint's color (`exhaustFor` repaints the racer's flame; `flameGeometry(racerId, hex)` caches one per color). The Buggy's engine sits lower so the driver shows over it from the chase camera. The alt paints, the drivers cut for the shared bodies and the showroom's shaders are prepared a piece at a time in idle time once the model files load (main.ts `warmLooks`), so neither the racer screen nor a race start waits on them.
- 2026-09-24: **The kart rig** (`rig.ts`), for the kart animation (kart-controller SOP, "View: the kart animation"). The model files are one fused surface (a tyre is not a part), so each racer's moving parts are five morph targets over soft regions of it. `lean`: the driver's upper body rolls toward +X about the hips. `look`: the head turns toward +X about an upright through its middle. `nod`: the head tips forward about the hips. `steer`: both front wheels turn toward +X about their own uprights. `heave`: the body and all on it rise on the springs (nothing up to the axles, all of it `RIG_SOFT.springs` 0.35 m above them, never the tyres), so a landing sinks the body onto wheels that stay on the road. Each target holds the tangent displacement of `RIG_UNIT` 0.001 rad (or m), so three's bounding box, which counts every target at full weight, grows by a millimetre; the geometry's `userData.rigUnit` tells KartView, which sets influence = angle / rigUnit. Soft masks: the driver is `DRIVER_CUTS`' box (its hip line faded over 0.05 below to 0.1 above, its sides over 0.05, the drop boxes subtracted); a wheel is a round of its radius to its tread (faded over the next 0.06 m), from its inner face outward, hub included. `MODEL_WHEELS` (front and rear) were measured from where each model's tyres touch the ground (the only part of a kart that does) and checked on renders with the wheels turned hard and the body dropped on them; the shared bodies' `BODY_WHEELS` are where bodies.ts builds them. The code-built karts rig too: the driver is every vertex from its first part (`ModelBuilder.driverPart`, `vertexStart`), pivoting at `racers.ts` hips, and the wheels come from its `WHEELS` table. A shared body's cut driver rides the springs whole. Templates are rigged once at load (`rigRacer`), a cut driver and a code-built model when made; clones share the targets and each has its own influences, so no material copies and no draw calls are added, and the shadow pass moves with them. Morph texture: five layers per racer model (about 0.8 MB of GPU memory each). The wheels do not spin (they cannot, fused to the body).
- 2026-09-24: Sprocket's model file was fitted 22.5° off +Z (its footprint's box is smallest at that turn, and its tyres' ground contacts square up there; found measuring the wheels for the rig), so he drove crabwise and was fitted about 14 % small. `manifest.json` yaw 0 → −0.3927. His `DRIVER_CUTS` box still holds the driver (checked in side and front sections of the cut).
- 2026-09-24: Boost flames are one jet mesh a kart for all its pipes (`flameGeometry(exhaust)`: per vertex the pipe's mouth, its direction, the offset across it, the share of its length and which pipe) drawn by a per-kart `flameMaterial()` whose uniforms carry each pipe's length, width, color and flicker (vfx-juice/flames.ts drives them). The shared additive vertex-colored flame material and the per-racer cone geometries are gone.
- 2026-09-24: Road wear and sheen (surfaces.ts `roadWear`, `ROAD_LOOKS`; critique: "flat, uniform gray with no specular or wear"), patched onto each track's road material after its lines through a new TrackAssets `road` hook (track-builder scene.ts, two lines): tonal patches in world space, two darker, polished bands where the karts run, grit near the camera, and where the biome suits them repaired rectangles and fine cracks (Harbor, Meadow, Canyon, a little Frostbite); Canyon's sand blown onto the edges, Frostbite's frost, Skyline's 12 m panels each a shade of its own with a joint between; the sun's glint (Blinn-Phong on the direct light, none in shadow, tighter and stronger on the bands); Boardwalk's night deck a wet sheen of violet sky at low angles over wet patches, and the neon edges' cyan light spilling onto the boards beside them. Same material, same draw calls.
- 2026-09-24: **Spectators** (`crowd.ts`, design §6 Spectators; review: the mid-ground by the road was empty). One shared critter rig (body, belly, head, snout, nose, eyes, ears, arms, feet, a hat's brim, crown and pompom, a collar, a stick and a flag) shaped into each species in the vertex shader from a table of seven vec4s (ear length, pinch and splay, stalk eyes, snout to beak, proportions, wings, hover) and painted from per-species and per-biome colour tables; a biome's rig carries only the parts its kit uses. Two InstancedMeshes: the full rig (about 340 triangles) for the stands' crowds, seen close from the grid, and a lite rig (about 150: fewer segments, no nose or feet) for everyone seen only at racing speed; the stands, bleachers, rope lines and cloud platforms one static toon mesh: three draws, no shadows. 105 to 130 critters a track at 1.45 × the rig (about 1.8 m: three silent Gemini reviews of race clips found smaller ones hard to read 22 m and more from the racing line), in view over 65 to 94 % of the lap (4 or more over 58 to 92 %); Frostbite 381 k triangles of 400 k, draws with shadows 55 to 78 of 80. Life is a pure function of the clock (`crowdPose` mirrors the shader for the checks): five idle styles (bounce, wave, pump, sway, hop), heads and bodies turning to the pack (the camera's view of the player's kart), a CROWD.cheer s burst when the pack passes a group's spot (32 trigger slots set from the meshes' onBeforeRender, as the sky life does), every group at the Final Lap Shift; shrinking away 110 to 140 m out; the governor's Low (no shadow map) sets the counts to 0. Only a glow stick lights itself; a night crowd takes a little self-light. Placed from the vista hook (`withCrowd` after buildVista: ctx.road, groundAt, clear): authored spots per biome (grandstands just past the start line, highlights), `corners` (the sharpest bends' outsides) and `lining` (a group about every 60 m where nobody watches), each searched along the road for level land CLEAR (1.6 m) or more past the course limit or solid edge, clear of props, the start gantry and the vista's perches, and of each biome's `keepOut` stretch where a shortcut leaves the road (the context knows only the main road); a pier's deck (no land function in the scene) is flat 18 m past the curb. crowd.test.ts: past every road's limit (shortcuts, final lap, Mirror), out of the chase camera's corridor and the loop camera, on the land as drawn, clear of the creature, draws and no shadows, determinism and bounds of the pose, the triggers, Low, disposal.
- 2026-09-25 (Final Lap Shift set pieces): Frostbite's lake on the snow (surfaces.ts `FROST_LAKE`, in the ground's and the land's snow shader, no draw of its own): open water with drifting floes until the shift, then ice spreading out from the crossing behind a bright front, its ragged shore kept clear of every course limit. Boardwalk's finale fireworks take turns on one 3.6 s beat (0, 1.2 and 2.4 s after the shift): with beats of their own (3.2, 3.7 and 4.1 s) they drifted into three flashes in 0.9 s, four with a lap-long point (WCAG 2.3.1 allows three).
- 2026-09-25: A Final Lap Shift sky can come in faster than SKY_FADE (`SKY_FADE_FOR`): the Meadow storm rolls over in 0.65 s, so its first bolt (0.7 s) lands under dark cloud, not a blue sky; the rest keep 1.5 s.
- 2026-09-25: The bunting and every other merged dressing prop take none of the sun on their faces turned from it, with no shadow lookup there (track-builder glow.ts `sunlessBackFaces`, on the dressing's toon): the faces toward the sun are shaded and shadowed as before, the faces away from it evenly in shade (the flags seen from behind read as backlit, a touch darker than the acne's average), no stripes. Checked on the way: no two spans stand at one spot (Harbor's nearest pair is 11 m apart, Mirror and the other tracks 12 m or more) and a span's flags never overlap, so nothing there could z-fight. Same draws, same materials. The shared vertex toon (toon.ts `vertexToon`) carries the same patch from the moment it is made (no user's shader changes mid-game): of its users only the podium's blocks take shadows, and their fronts streaked the same way.
- 2026-09-25: **Racers built from parts** (`rigged.ts`; Adam: "the wheels don't even spin", "the characters don't move like a 3D character"). A racer listed in `public/models/racers/manifest.json` loads three files in its one turn of the load line (RacerModels: parts first, then its fused file if a part fails, then the code-built kart): a skinned driver in A-pose (Meshy auto-rig, 24 bones), the kart body with empty wheel arches (Tripo, yaw −π/2) and one wheel (axle on X, rim on +X). At load they are fitted into the kart's frame (+Z forward, +X the driver's own left, screen-left from behind) and merged into ONE SkinnedMesh: the body on a `body` bone (the springs' heave), each wheel on a `hub` + `wheel` bone pair (roll, steer, bob), the −X wheels mirrored with their winding flipped so they still face out, the driver's rest pose baked from the file's own skinning (one blend of per-bone matrices a vertex) onto its 24 bones rebuilt at unit scale under `body`. The three textures share one canvas atlas (2048×1536: driver and body at 1024, the wheel at 512, a row of 16 px color swatches for code-built attachments; flipY off, as glTF's). One material, `MeshStandardMaterial` at roughness 0.82, metalness 0, no emission: PBR as the fused files were (kept PBR, not toon, 25 Sept 2026; Meshy's driver comes with its color as emission and specular 2, which would glow). So a kart is one draw call and one shadow draw, as a fused file was; each kart is a SkeletonUtils clone (geometry and material shared, bones its own, ~0.2 ms). Build cost on the M4 Pro: the atlas 2 ms, the template 7 ms (17 ms on its first run), in two tasks (a racer can land mid-flight in a course intro).
- 2026-09-25: **Seated by IK on the kart's points** (25 Sept 2026, so any driver can sit in any kart): `body.seat` (the hip point), `body.grips` (the hands, at ten and two), `body.feet`, in the body's fitted frame; `seatDriver` puts the Hips on the seat, leans the spine `SEATED.lean` 0.1 rad forward (more, up to 0.52, while a grip is out of reach), two-bone IK for each arm (the wrist `wrist` 0.07 m short of the grip, elbows out and down) and each leg (knees up and out). Any driver sits again in another kart from its rest pose (`RiggedTemplate.rest`). The shared bodies seat it too (`bodies.ts SEATS`: Classic hips 0.5 m, Buggy 0.56 m, hands on their small wheels, feet in the nose), the driver alone as a second skinned mesh beside the code-built body: two draws, as a cut driver was. `driver.seat` is used only for a body with no seat. Per-racer differences go in `RACER_POSES` (lean, poles, wrist, scale, extra turns); Juniper needs none.
- 2026-09-25: **The steering wheel turns**: `body.steering` (center, the column's axis toward the driver, the rim's radius) cuts it out of the body onto a `steer` bone: the body's vertices within 4 cm of its plane and 4.5 cm past its rim that are dark in the body's texture (read at 256 px; `STEER_CUT`). Juniper's, measured by fitting a circle to the rim's vertices near its plane: center [0, 0.74, 0.281], axis [0.01, 0.615, −0.788], radius 0.145 (225 vertices move). The grips turn with it about the column, so the hands go round with the wheel.
- 2026-09-25: **Pipe mouths** (Adam: "I don't remember seeing any pipes"; the flames' anchor interface, `Exhaust` ports + dir, now with an optional `splay`, 0 for measured pipes): `body.exhaust` in the manifest. Juniper's two chrome stacks stand at the back on the −X side (the driver's right; screen-right seen from behind), found by the texture's chrome (light, gray) vertices at the back: tall pipe mouth [−0.38, 0.79, −0.955], short pipe [−0.444, 0.713, −0.952], both pointing [−0.24, 0.966, −0.09] (up and a little out and back, the pipes' own axis near their tops). The flames hang off the `body` bone (`userData.exhaustAnchor`), so they ride the springs' heave as well as the chassis's roll and pitch; the flame look itself is untouched (vfx-juice).
- 2026-09-25: Part files budget (rigged.test.ts, on every racer in the manifest): driver ≤ 20 k triangles, body ≤ 8 k, wheel ≤ 2.5 k. `scripts/models/racer-parts.sh <id>` shrinks a racer's three files in place with gltf-transform (dequantize, weld, simplify, quantize: skin attributes kept, only the index changes): driver ratio 0.5 at error 0.0015 (Juniper 24.9 k → 19.0 k; at 0.02 the face's UV seams wobbled on a close-up), body 0.3 at 0.004 (22.8 k → 6.8 k, no visible change), wheel 0.3 at 0.004 (2.8 k → 2.35 k). A kart is 35.2 k triangles (the fused files were about 11 k), 1.5 MB of files (2.0 MB before). meshopt's Permissive mode reached 9 k on the driver but smeared the face's texture across its UV seams: not used. Run the script on each racer's files as they land (the budget test fails until it is).
- 2026-09-25: Alt paints repaint a rigged racer's one atlas (driver and kart together, as the fused files' one texture was) through the same rules, once, into one shared material; Juniper has no alt paint, so nothing changes today, and the rules for Pip, Boulder and Sprocket were tuned on the fused textures: check them on the new ones when those racers land. Attachments (`driver.attachments`: a code-built cone on a named bone, its color a swatch in the atlas) merge into the same mesh at no draw; none ship (Pip's beak waits for his new model).
- 2026-09-25: Tried and dropped: a far level of detail built at load by vertex clustering over the same vertices (a second index, three's LOD). Clustered within UV islands it cracked open along the seams; across them it smeared the texture into stripes; and eight karts at full detail measured within noise of the fused files on the M4 Pro (performance SOP). A proper far level would be a meshopt index made offline and shipped in the part files.
- 2026-09-25 (look review: "not on the Mario Kart World level"): a stylized-PBR look prototype behind `?look=pbr` (look.ts; DEFAULT_LOOK stays 'toon', whose shaders are as they were). Every toon material in a track scene gets a MeshStandardMaterial twin (its colour, maps and shader patches; roughness 0.9, no metal); on the world's own surfaces the sun is gained 2.0x and wrapped 0.3 (the toon ramp lit a floor fully under a 24° sun, a physical floor gets sin 24° of it) and the fill 0.6x; the painted sky is a PMREM map (SkyEnvironment: the dome's own material round a cube camera, 256, painted again per race, when its painting lands and three times through a shift's fade, 0.3-0.8 ms on the M4 Pro), 45 % saturated, 0.4 on props, 0.22 on the road, 0.26 on lawns. The road (surfaces.ts roadDetail): the asphalt's own relief (detail.ts: a Sobel of the albedo's luminance at load, sampled in world space, mirrored with the tile) in its normal and roughness, the racing line a little darker and smoother with faint tire marks, worn paint, red-and-white curbs on the inside of tight corners. The land and ground: two-octave world-space variation, their textures' relief, a colour lift, less sheen, a soft dirt edge by the curb on a lawn. grass.ts: one InstancedMesh of 13-triangle tufts with a few flowers along the curbs (Harbor, Meadow), fading out 30-52 m from the lens, in place of the verge tufts (the flower clumps stay for their colour). Karts are untouched: the rigged racers keep scene.environment; `worldEnvironment()` is there for them.

- 2026-09-25 (distance haze, Adam: "will items off in the distance have natural fading ... how does Mario Kart World work with that?"; a weakness the same day: the painted skies' pale horizons washed the distance out flat, and the lawn past about 40 m read as painted paper): atmospheric perspective for every world PBR material, one shared patch (look.ts `HAZE`, `applyHaze`, run from `lookLights`, so ground, road, land, decor and the far vista's own buildings and mountains all haze the same way; vista.ts's movers and glows, on their own custom shader, take the identical formula by hand since `applyLook` leaves custom shaders alone). Two things three's own fog chunk does not do, both cheap (one varying, one `pow`, one `exp`, no new pass or texture):
  - an accelerating curve (`HAZE.pow`, 2.2), not a straight ramp, so the middle distance stays readable and the wash is spent mostly on the far edge — three's own `FogExp2` docs give the reason (threejs.org/docs/#api/en/scenes/FogExp2: "a clear view near the camera and a faster than exponentially growing density as distance increases"); Carsten Wenzel (Crytek), "Real-time Atmospheric Effects in Games" (SIGGRAPH 2006 Advances in Real-Time Rendering course) ships the same height/distance exponential recipe in CryEngine2;
  - a height term (`HAZE.heightMin`/`heightFalloff`) so a peak keeps its own colour while its foot melts into the horizon: Inigo Quilez, "Fog" (iquilezles.org/articles/fog): real air is denser at low altitude, `d(y) = a·e^(−b·y)`, integrated along the ray.
  Still just three's own `fogColor` (the sky's own horizon, sky.ts) — the far road still melts into the sky exactly as the SOP's Approach describes, only later and less flatly; `?look=toon` never compiles it (look.test.ts). `hazeGroundY` (a material's `userData.hazeGroundY`, else 0) is close enough to right for every track but Skyline, which has none and stands high everywhere (vista.ts grounds its own copy at the road's own lowest point, `ctx.roadMinY`, matching `Vista.floor`); a future per-track override would need a hook through `TrackAssets`, not added today.
  Found and fixed alongside it: the ground, the land and the road's own painted relief (surfaces.ts `pbrGround`, `pbrCoast`, `roadDetail`) faded to dead flat by 60 m / 40 m of view-space distance — well short of a long straight or a wide view, and long before the scene's own fog even starts (140 m, main.ts) — so there was a band of flat, unhazed colour between the two. `GROUND_RELIEF_FAR` and `ROAD_RELIEF_FAR` now hold it to 320 m / 170 m; the new haze curve, not flatness, carries what is left.
  Two vista set-pieces read as flat once the haze stopped hiding them (screenshots against the MKW reference frames): Meadow's patchwork fields were plain colour blocks (now furrowed, ploughed along X or Z so neighbouring plots read differently, with real hedgerow bush clumps in place of bare posts) and Skyline's floating islands were a squashed, near-flat green ball (now hummocked, with a visible rock lip, root strata and taller trees) — both still one draw, no new texture, no LOD tier added (the SOP's own 25 Sept note above already tried and dropped a clustered far level; the flatness here was a rendering problem, not a missing level of detail). A dithered LOD cross-fade (Digital Foundry's read of MKW's own pop-in; the same technique in Unity's and Cesium's LOD docs) stays the right tool if a future far level is ever added, so nothing pops.

## Lessons (repair loop writes here)
_(error → cause → fix → rule; newest first)_
- 2026-09-25: **Two bunting flags near the finish camera drew a sawtooth tear (review: "z-fighting or shadow acne").** Cause: shadow acne. three draws a caster's back faces into the shadow map, so a face turned from the sun is tested against its own depth (6 cm texels, a one-texel normal bias, a −4 cm depth bias), and MeshToonMaterial's ramp still gives a face with dot(N, L) under zero a third of the sun, so the lookup's texel-sized flips show; on a flag, a 5 cm card at 43° to Harbor's sun, they were diagonal bands a texel wide, and the rope and the life-ring badge had them too. The instanced props take no shadows, so only the merged dressing (it does) showed it. Found by photographing the flags from 3 m with the dressing's receiveShadow off (clean) and the normal bias raised (clean). Fix: glow.ts `sunlessBackFaces` on the dressing. Rule: a toon material that takes shadows leaves its sun-averted faces out of the lookup; look at a thin card from 3 m, not only from the racing line.
- 2026-09-24: **Sprocket drove 22.5° crabwise; nobody saw it for a day.** Cause: an image-to-3D model built from a three-quarter concept image comes out turned by a fraction of a turn; the manifest's yaw fixed only its facing (quarter turns), and a turned box only fits smaller. Fix: yaw −0.3927. Rule: after adding a model file, check that its tyres' ground contacts (y < 0.05) square up with the axes (the smallest footprint box at 0°), not only that it faces +Z.
- 2026-09-24: **White gull-folk, sheep and cloud sprites bloomed like lamps in the crowd (headless close-up).** Cause: a per-instance fur tint (±10 %) pushed near-white colours past 1.0, and the glow rule lights any colour past white. Fix: tinted colours are clamped to 1; only a glow stick (its own flag) lights itself. Rule: anything that scales a colour clamps it under white unless it means to glow.
- 2026-09-24: **Boardwalk's first stands stood 1.1 m down in the pier's deck.** Cause: the scene gives the vista context no land function on a walled sea track, so `ctx.groundAt` answered the sea's height. Fix: crowd.ts models the pier's deck (flat 18 m past the curb, 0.52 m under the road, as land.ts draws it); crowd.test.ts compares every critter's feet with the coast mesh as drawn. Rule: check what a context's ground answers on each kind of track (off-road land, pier, sky) before standing anything on it.
- 2026-09-24: **A white see-through slab filled the left third of the screen on Canyon Rush (t 0.15).** Cause: the chase camera passes within a metre or two of the geyser beside the road, and the eruption column (additive, double-sided) washed the view white from inside; and props were placed by their box's half-width, so a square one turned 45° (a chalet's eaves, a house) reached √2 further, over the course limit into the lens's path. Fix: vent columns and pools fade out within 2 m of the lens (whole by 6 m); props are placed by their widest reach from their own axis; game/corridor.test.ts sweeps the lens over every drivable metre of every road at chase height and at its lowest and fails if any decor or dressing volume holds it. Rule: anything the lens can meet fades out completely near it; place by true radial reach, never by box half-width.
- 2026-09-24: **Every Boulder, Pip and Sprocket in the race wore the alt paint once one player chose it (seen in a race screenshot).** Cause: `Texture.clone()` shares its `Source` with the original, so setting the clone's image repainted the racer's own texture too. Fix: `textureWithImage` gives the copy a `new Source(image)`; a test checks the original's pixels are untouched. Rule: never set `.image` on a cloned texture; give it its own Source.
- 2026-09-23: **Skyline Circuit looked like a flat peach desert.** Cause: the sky gradient was keyed to the dome's centre at the world origin; with the road 40–95 m up, the whole view sat in the horizon band, and the hemisphere light lit cloud undersides earth brown. Fix: the dome follows the camera, the dawn preset has a blue top and a blue haze below, and no-ground tracks bounce the sky colour. Rule: anything that paints by direction must be centred on the eye; check a new track at its real height, not at y 0.
- 2026-09-23: **A commit said "verify green" with one test failing.** Cause: `npm run verify | grep …` reports grep's exit code, not the test run's. Fix: amended before any push; verify now runs to a file and its own exit code is checked. Rule: never pipe the gate into a filter when its exit code decides a commit.
- 2026-09-23: **The first dressed track rendered a million triangles.** Cause: a 180-triangle outlined bollard instanced 1,188 times is 428 k before shadows. Fix: count instances before choosing detail. Rule: budget triangles per *instance* in a test, and measure `renderer.info` after every art change.
- 2026-09-23: Two `add-prop.sh` runs at once both rewrote `public/models/props.json` and one entry was lost. Cause: each run reads, edits and writes the whole manifest. Fix: re-added the entry by hand. Rule: run `add-prop.sh` one at a time (downloads may run in parallel, the manifest write may not).
- 2026-09-23: Tripo at `texture_quality: detailed` with no face limit gave 1.5 M triangles (32 MB) per creature. Cause: adaptive geometry. Fix: dequantize, weld, `simplify --ratio 0.017 --error 0.02`, quantize: 25 k triangles, 0.5 to 0.65 MB. Rule: check the triangle count of every new model before it ships; hero models stay near 25 k, repeated props near 1.5 k.
